# 待自查升级

记录**已知不够完善、依赖私有接口或缺少验证**的技术点。每条都应写明：现状、为什么不理想、下一步怎么自查或升级。

---

## 1. 激活应用后「兜底回切桌面」

**现状**：`ActivatedAppMover` 在激活时把应用窗口搬到当前桌面（`CGSProcessAssignToSpace`）。
少数应用（实测 ChatGPT / `com.openai.codex`）给自己的窗口设了
`NSWindowCollectionBehavior.moveToActiveSpace`——激活时**应用自己**把桌面拉过去。
外部无法改写该行为（AX 窗口属性里没有 collection behavior，AppKit 没有跨进程接口），
因此在搬移完成后延迟 0.4 秒再检查一次：若桌面已被带走，用
`CGSManagedDisplaySetCurrentSpace` 切回用户原本所在的桌面。

**为什么不理想**：

- **用户会看到一次桌面闪动**：先被应用拉到它的桌面，再被切回来。这是妥协方案，
  `AGENTS.md` 原本禁止「先跳过去再切回来」，此处经用户临时授权。
- **靠时间窗猜测**：0.4 秒是实测值（用户日志里跳转发生在搬移后约 62ms），
  属于经验常数而非协议保证。应用若更晚抢桌面，回切就会漏掉。
- **两个私有符号**：`CGSProcessAssignToSpace`（搬移）与 `CGSManagedDisplaySetCurrentSpace`
  （回切）。均在 macOS 27.0 (26A428) 实测可用，不保证未来版本。
- **回切不做归属复核**：只比对 `CGSGetActiveSpace` 的值，切回后没有二次确认。

**是否有更优雅的解法（待自查）**：

1. **在激活之前就绑定**：系统 Dock 的「选项 → 分配给 → 这个桌面」似乎写入的是**持久绑定**，
   而不是一次性的窗口归属。若它的效果强于 `CGSProcessAssignToSpace`（能让
   `moveToActiveSpace` 应用不再抢桌面），则应当复刻这条路径。Dock 二进制里出现
   `CGSSessionSetCurrentSessionWorkspaceApplicationBindings`，签名未知，**下一步**：
   反汇编 Dock 里该符号的调用点确定参数类型，或观察「分配给」写入前后窗口服务器状态的差异。
   注意：DX 上「回切」比「预先绑定」差得多——绑定没有闪动。
2. **在激活前搬移**：若某个通知能早于应用的抢占动作到达，就能在它之前把窗口放好，
   从根本上不需要回切。**下一步**：测量 `didActivateApplicationNotification` 与
   应用抢占动作的先后（本仓库已有 `/tmp/ps-window-layout.log` 时序记录）。
3. **切换前拦截**：Dock 二进制里有 `SpaceSwitchTransition` / `SpaceSwitcher` 相关类型，
   但没有可用的公开或已知私有通知在切换**前**发出。**下一步**：确认是否存在
   `com.apple.spaces` 的分布式通知。
4. **让用户改用 PS 坞点击**：PS 坞点击走自己的路径，实测不触发应用抢占，因此不闪动。
   这不能算技术解法，但可以作为产品层面的建议写进设置说明。

**如何自查现状是否仍成立**：

```bash
# 开关必须先打开
cd ~/projects/powerspaces
rm -f /tmp/ps-window-layout.log
# 然后复现：目标应用只留在桌面 A，切到桌面 B，用 Spotlight 打开它
grep -E "NOTIFY app=|outcome" /tmp/ps-window-layout.log
```

- `outcome=moved` → 桌面没被带走（应用规矩）
- `outcome=movedAndSwitchedBack` → 发生了回切（`currentSpace()` 在搬移后已是被抢走的桌面）
- 若日志里出现 `SPACE-CHANGED-NOTIFY` 紧跟 `NOTIFY` → 说明应用确实抢了桌面

---

## 2. 「激活应用」到底该监听什么

**现状**：两条路并存——

1. **通知**（`NSWorkspace.didActivateApplicationNotification`）：主路径。
   实测它比系统切桌面**早约 62ms** 到达，因此能在应用抢桌面之前完成搬移。
2. **轮询**（既有 `pollTick` 里比较相邻两次前台 pid）：兜底。
   只在通知不送达时起作用，间隔取用户的 `pollInterval`（默认 2 秒）。

**为什么不理想**：

- **通知不覆盖"同一应用重复激活"**：实测同一应用已是前台时不再发通知。
  这时若窗口在别处（例如被手动搬走），通知路径不会触发，只能等轮询。
- **轮询太慢**：默认 2 秒。用户实测失败的那次正是**只有轮询**的版本——
  系统先把桌面切走，轮询醒来时判定「切桌面了→不搬」。
- **两套判定并存**，`spaceRecentlyChanged` 的语义在两条路径下并不完全一致
  （通知路径下它是"上次判定所在桌面"，轮询路径下还兼作切桌面检测）。

**最优雅的目标形态（待自查）**：

- **只保留通知 + 一个极轻的"补漏"触发**。理想触发源应满足：① 在应用抢桌面之前到达；
  ② 覆盖同一应用重复激活；③ 不新增常驻轮询。
  候选：`NSWorkspace.didActivateApplicationNotification` + 应用自身的 AX
  `AXWindowCreated`/`AXFocusedWindowChanged` 观察（`AXObserver` 已在
  `NotificationBadgeStore` 里用过，属于既有模式）。
- **去掉轮询里的激活检测**，让它只负责 Dock 内容刷新，职责更单一。
  **下一步**：确认 `AXObserver` 的 `kAXFocusedWindowChangedNotification` 能否在
  「同一应用重复激活」时触发；若能，它就是比轮询更合适的补漏源。
- **量化开销**：目标是在空闲时不产生任何额外唤醒。当前轮询路径每次唤醒要读一次
  `frontmostApplication`（廉价），但配合 `snapshot()` 就不是免费的。
  **下一步**：测量通知路径与轮询路径各自每次激活的 `snapshot()` 调用次数。

**如何自查现状是否仍成立**：

```bash
grep -cE "NOTIFY app=" /tmp/ps-window-layout.log      # 通知路径触发次数
grep -cE "activated-app app=" /tmp/ps-window-layout.log # 轮询路径触发次数
```

若后者明显多于前者，说明通知覆盖不足，需要补漏源。
