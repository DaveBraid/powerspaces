# 开发经验

只维护影响后续开发的重要、可复用结论，不记录过程流水账或未经验证的猜测。

## 原生玻璃

- `NSGlassEffectView` 应通过 `contentView` 承载前景。把玻璃当作独立背景层会绕开系统对内容的自适应处理；移动层级时必须复核图标悬停溢出、拖放和空白区菜单。
- `alphaValue` 淡化整个合成结果，边缘高光与前景也会受影响。需要保留光学轮廓时，使用原生材质与 `tintColor` 调节染色，并在界面中准确区分“着色强度”和“不透明度”。
- `.regular` 和 `.clear` 都是原生玻璃；前者强调内容可读性，后者更通透。不要把 `.regular` 称为普通模糊，也不要未经对照断言系统 Dock 的私有实现。
- 本机 macOS 27 SDK 没有公开透光率参数，但实测内部 `glassBackground` 的 `inputBlurRadius` 与 `inputFaceOpacity` 可独立减弱背景模糊和底色；保持折射和整层 alpha 原值；原生高光宽度与强度保持不变。私有实现集中于 `GlassBackgroundTuning.swift`，限定系统大版本、探测键与数值类型、复制滤镜再修改；记录系统原值避免滑块反复调整造成累积衰减。
- **高光根因修正（macOS 27.0 / 26A428）**：不能把背景滤镜 `inputKeyFillHighlightHeight`／`Amount` 的可写性当成原生 Dock 上下高光的因果证据。同一 clear 玻璃窗口的实机激活／失活对照显示：独立 `CASDFKeyFillHighlightEffect` 所在 `CASDFLayer.opacity` 从 1 变为 0，`keyColor`／`fillColor` 的 alpha 同时从 1 变为 0；视觉上上下高光随之消失。PS 使用 `.nonactivatingPanel`，仅调背景滤镜无法解决前景高光失活。正式实现已切换到 `.dock` 配方与渲染环境覆盖，保留非抢焦点行为。
- 系统 Dock 的静态构造链已核对：`ModernFloorLayer` → `GlassMaterialProvider.Configuration.dock` → `DLMaterialLayerController`，与公开 `.clear` 配方不同；渲染器支持独立 SDF 玻璃高光。二进制也有 legacy／其他组件的 rim 控制器，不能把所有 rim 符号都归到现代底栏。尚未读取系统 Dock 实时图层，不能声称掌握其完整光效参数。
- AppKit 会重建内部滤镜。通过布局后合并更新及滤镜变化观察重新应用，不能以持续轮询保活；0% 恢复当前系统原值，未知实现不写入。开发验收既要检查参数，也要固定背景做视觉对照。
- macOS 27 的 `effectIsInteractive` 控制交互响应，不是折射开关。单块玻璃不需要为了“效果齐全”而添加 `NSGlassEffectContainerView`。
- 光学验收应固定背景、尺寸、缩放与强度，观察高反差边界形变，再验证非激活窗口、悬停和四向停靠。编译成功与 API 可用均不能代替视觉验收。
- AppKit 的 `NSView.hitTest` 接受父视图坐标。跨容器转发时不要重复转换到子视图局部坐标；同时验证图标内与图标外的命中结果。
- 降低透明度时切换为不透明背景，前景必须继续显示；玻璃与纯色切换时要解除并恢复 `contentView` 归属，不能只隐藏整个玻璃视图。

## 构建与本地化

- 本机 Swift 6.4 默认 swiftbuild 后端曾把 SDK 标记写成部署版本 14.0，使控件回退旧外观。当前使用 native 后端；升级工具链后检查 `LC_BUILD_VERSION` 再移除兼容，不要修改二进制伪造 SDK。
- native 与 swiftbuild 的语言资源目录大小写不同；本地化查找兼容 `zh-Hans` 和 `zh-hans`，必须验证打包后的应用。
- 本机 CLT 未提供 SwiftUIMacros 插件，使用 `ViewState` 别名明确选择原有属性包装器。

依据：[Apple AppKit 接入说明](https://developer.apple.com/videos/play/wwdc2025/310/)、[Apple Liquid Glass 材质说明](https://developer.apple.com/videos/play/wwdc2025/219/)，以及本机 SDK 头文件。

## 应用生命周期

- 程序坞与状态栏的退出菜单共用 `ApplicationActions`，先结束菜单跟踪，再在主队列调用 `NSApp.terminate`。系统 Dock 和快捷键的恢复集中保留在 `applicationWillTerminate`，不要复制清理代码或使用强制结束代替正常退出。
- 原生菜单跟踪期间，冻结宿主 Dock 的列表重建、位置调整和自动隐藏，避免后台刷新拆掉菜单锚点，造成“点击无效”。菜单关闭后恢复正常刷新。

### 程序坞分区与无窗口归属

- 固定项的运行状态来自全局进程表，当前桌面的窗口列表单独维护，避免把其他桌面正在运行的固定项误画成未启动。
- 无窗口进程没有系统 Space 归属；按窗口编号记录桌面 UUID 归属并按进程启动身份持久化，退出清理；首次无历史的无窗口进程归入活动桌面。不能声称这是系统提供的精确归属。同一窗口移动要替换旧归属，不能累计“访问过的桌面”；关闭才保留最后归属。短暂快照缺失保留窗口编号，重新出现仍可修正；最多保留 256 个已关闭窗口编号，较旧记录压缩为桌面集合。候选列表必须包含已归属的所有存活进程，不能用“全局无窗口”过滤，否则其他桌面有窗口时会丢失本桌面图标；多进程应用按窗口 PID 记录归属、按应用键去重显示。
- `NSButtonCell` 的默认图文分配会受图片尺寸影响；明确实现 `imageRect` / `titleRect`，并按实际文本宽度分配按钮空间。图标缩放使用鼠标事件，距离基准固定在一次悬停开始时，避免扩容后重新取中心造成反馈抖动。
- 动态扩容必须同时更新窗口、玻璃和原生 contentView。实机曾测到外层宽 694 点、前景仅 629 点，造成右侧标题裁切；仅加宽按钮无法修复。
- 缩放时不能用变化中的玻璃中心定位图标组；应固定外侧边缘约束，圆点使用独立几何图形并按实际图标中心定位。四边停靠的实测应分别比较屏幕 y / x 坐标，避免字体字形基线造成假对齐。

### 原生 Dock 配方的正式接入（26A428）

- 独立原型及失活日志证实 `DesignLibrary.GlassMaterialProvider.Configuration.dock` 可通过 SwiftUI Material 渲染；关键是私有 `EnvironmentValues.windowAppearsActive = true`。普通 `appearsActive`、`materialActiveAppearance(.active)` 不等价；不得用激活 PS 或伪造 keyWindow 来补偿。
- `NativeDockMaterial.swift` 只托管背景。Swift 私有调用隔离在 `CDockMaterial`，使用 Clang 的 `swiftcall`、`swift_indirect_result`、`swift_context` 标注真实调用约定；所有函数动态解析，不静态绑定私有 setter。参见 [Clang 调用约定说明](https://clang.llvm.org/docs/AttributeReference.html#swiftcall)。初始化器消费输入值，释放原始存储时不能再次析构。
- 只启用 Apple Silicon / macOS 27.0 构建 `26A428`，同时检查完整元数据、Configuration 216 字节、Provider 328 字节、Material 17 字节／stride 24、EnvironmentValues 16 字节及 8 字节对齐、MaterialProvider 见证表、环境 false/true 回读。系统升级后先重新验证，不能只扩大版本条件。
- 渲染后再核对真实 `CASDFKeyFillHighlightEffect` 的图层 opacity、颜色 alpha 和背景滤镜，最多三次延后验证；失败则永久回退该视图的公开玻璃。背景滤镜和渲染树通过观察与布局事件维护，高光仅做只读验证，无持续轮询。
- 正式桥接实测：应用后台／非 key 窗口、480→580 点宽度、浅→深色后高光保持；透光率 0／40／80／100% 及恢复原值通过；原生高光强度和宽度不变、AppKit 命中、纯色切换与公开材质回退通过。此结论不等于取得系统 Dock 的完整实时参数。

- 从 `NSGlassEffectView.contentView` 迁回普通 AppKit 容器时，恢复 `translatesAutoresizingMaskIntoConstraints` 和尺寸管理；原生容器会改变布局模式，仅设置 frame 会导致前景偏移。用同尺寸双材质实机对照及容器 bounds 检查验证。

- 整体黑白适配使用经本机验证的 `CABackdropLayer.tracksLuma`：`captureOnly` 不绘制背景，通过 `backdropLayer:didChangeLuma:`（Double）获得区域平均亮度，0.43／0.57 迟滞阈值避免抖动；不截图或增加应用侧轮询。探测失败或受保护背景回退外观黑白色。逐像素 `vibrantColorMatrix` 会导致同一文字或线条内部混色，不能用于整体选色。采样层必须在文字／标记层下方作为兄弟层，避免采到元素自身，尤其是细线。分割线独立约束到玻璃中心，避免继承图标沿屏幕边缘的对齐。

- `NSTextField` 默认 alignmentRectInsets 左右各 2 点；几何标记继承它并使用自动布局后，1.5 点宽度约束会产生 5.5 点绘制宽度。标记覆盖为零边距，验证实际屏幕矩形的宽度／高度，不能只检查约束常数与中心。

- 桌面归属修复的实机验证：独立普通应用关闭唯一窗口后仍显示在未固定区；保持该进程存活并重启 PS，图标和保存的归属均恢复。跨桌面累计、多进程去重、PID 复用及退出清理另由假数据测试覆盖。首次升级无法恢复未曾保存的历史归属。

- 移窗隔离修复实机：独立测试窗口在两块显示器的不同 Space 间移动，保持同一窗口编号，归属从 A 替换为 B，A 的图标消失；随后关闭窗口仍仅保留 B。隐藏桌面 UUID 来自完整 Spaces 列表，避免只有访问目标桌面后才更新；同显示器移窗路径以假数据覆盖，未操作用户窗口做实测。

- 缩放算法采用用户验证的 `/tmp/ps-dock-magnification-study/Preview.swift`：三倍基础尺寸影响半径、正弦映射左右边界、余弦进出以及 `floor(42 * log(尺寸差 / 2 + 1) + 1)` 毫秒时长。正式版纯函数在 `DockMagnification.swift`，不依赖临时原型文件，也不调用系统 Dock 的私有动画入口。所有几何使用一次悬停开始时的屏幕坐标，图标与间隙同步映射；禁用叠加隐式动画。待处理输入与进出过渡由同一显示链接驱动，稳态暂停回调，拖拽／重建／隐藏先复位；自动隐藏命中区使用变形后的实际窗口位置。

- 玻璃固定厚度后，放大图标会超出材质边界。实测 AppKit 图层重建会恢复宿主 `clipsToBounds`／`masksToBounds`，需在 Dock 宿主布局时保持前景不裁切；四向几何测试同时检查裁切标志，另以纯图标预览检查实际上沿。

- 程序坞缩放会改变窗口及 tracking area，快速跨入其他应用时仅依赖 mouseExited 可能遗漏收尾。复用本地／全局鼠标事件监听，在收到窗口外事件时启动余弦退出；即使关闭自动隐藏，开启缩放仍需该监听。仅处理已有缩放，不增加轮询。

### 原生工作区预留实验（27.0 / 26A428）

- `SLSSetDockRectWithOrientation` 可改变服务器保存的 Dock 矩形，但不等于已运行应用刷新工作区：底部预留 79→119 点，新进程的 `NSScreen.visibleFrame` 和测试窗口 zoom 上移 40 点；提前启动的独立 AppKit 进程即使用标准 `NSApplication.run`，仍保持 79 点。重新取得 `NSScreen.screens` 也无效。不要以新进程验证替代已运行应用验证。
- 核对本机系统 Dock 二进制：矩形变化发送 `0x4b1` 广播，载荷为四个 Float（x/y/width/height）加两个 UInt32（reason/orientation），共 24 字节，随后调用上述 setter；原型载荷与顺序并未漏掉这一通知。AppKit 也注册了 `0x4b1`。
- 根因证据：`SLSPostBroadcastNotification`（`CGSPostBroadcastNotification` 的同一入口）只发单向 Mach 消息；返回 0 是发送成功。WindowServer 的 `_CGXPostBroadcastNotification` 对连接资格做检查，普通实验进程未获投递；只重发当前矩形、给同一消息补回复端口后，实测 `transport=0 reply_id=30222 server_result=1000 (0x3e8)`。不可把传输成功当作服务端授权成功，也不能仅按符号存在启用功能。
- 原型与日志保留在 `/tmp/ps-dock-workarea-study/`：`observer.m` / `observer-run.log`（提前启动、标准事件循环）、`external-run.log`（有独立看门狗恢复）、`ack.m` / `ack.log`（实际拒绝码）；临时路径非仓库依赖。回复诊断使用本机构建反汇编核对的私有函数相对地址，只可用于这次实验，不能进入生产代码。
- 所有测试已恢复底部 79 点并由独立进程回读。未验证其他方向、自动隐藏和普通应用；第二屏仅确认未受底部实验影响，不代表支持逐屏预留。当前不接入仅改变服务器、无法同步现有应用的半成品，也不接管原生 Dock 的特权连接；待找到并验证允许 PS 使用的刷新路径后再实现版本探测、恢复和失败回退。正式 PS 无此工作区写入逻辑。

- 后续刷新路径排查（同一构建）：公开显示配置事务提交原有显示器坐标，返回成功但提前运行的 AppKit 观察进程仍保留旧工作区；未改变分辨率／排列，实验后独立回读恢复 79 点。日志为 `noop.log`、`observer-noop.log`。事务作用域参见 [Apple CGConfigureOption](https://developer.apple.com/documentation/coregraphics/cgconfigureoption)，不能靠成功返回判断缓存已刷新。
- `SLSSpaceSetEdgeReservation` 的 WindowServer fallback `_XSpaceSetEdgeReservation` 仅在空间是自身所属平铺管理对象且其类型为 4 时更新预留，否则也返回 0；本机普通桌面只读类型为 0。此入口不是已验证的普通桌面通用预留接口；现代 bridged 路径尚未证明可用于普通桌面，不调用未知参数污染用户空间。
- 系统 Dock 在传统矩形广播后还向 `WindowManager.ExposeCoordinator.setDockInfo` 传递显示器、当前／最大 inset 和方向；该路线经 `AdminXPCConnection.xpcSetDockInfo`，`AdminXPCListener` 在接受连接前检查 `com.apple.private.windowmanager` entitlement。只读反汇编确认该检查，没有尝试伪造签名或取得该权限；不能把此入口当作第三方可用替代通知。

- 通知徽章缓存必须区分读取失败与明确空字符串：失败保留旧值，完整列表成功后才清理消失项；同 bundle 的多个源若冲突则保持未知，禁止按显示名称或窗口数量生成通知数。覆盖层用 NSButtonCell.imageRect 定位，避免标题宽度改变徽章位置；红色通知与蓝灰窗口数上下分离。
- 当前徽章实机验证阻塞于辅助功能授权：命令行原型及原安装路径的 PS 只读诊断均报告 AXIsProcessTrusted=false，AX 读取返回 -25211；已验证缓存和静态徽章布局，尚未验证系统 Dock AXStatusLabel 的实际内容、变化通知、隐藏／重启和应用退出。启用并确认授权后必须继续实测，不能把静态预览当成来源验证。


### 本地更新与辅助功能签名身份

- 临时签名的 designated requirement 是 `cdhash`，内容变化会改变身份；保持 bundle ID／路径无法独自解决旧授权失效。
- 固定自签名证书产生“bundle ID + certificate leaf SHA-1”要求，不依赖构建哈希；密钥放在仓库外的独立钥匙串，权限限制为当前用户，不添加系统证书信任。签名配置存在而不可用时必须停止，不能悄悄回退。
- 依据：[Apple TN2206](https://developer.apple.com/library/archive/technotes/tn2206/)。首次身份迁移需用户重新授权；相同签名要求的验证不能替代真实 TCC 跨版本测试。


### 图标居中与运行标记

- 程序坞厚度变化时，静止内容外沿应为（玻璃厚度－静止内容厚度）／2，不能固定为圆点留白；内容建立、标题宽度变化后必须同时刷新厚度和定位约束。
- 左右停靠时图标与展开标题作为整体居中；通过平移对齐框处理不同宽度，运行圆点反向补偿该位移以保持公共基线。放大仍使用静止外沿，不能用放大后的尺寸重新计算静止中心。


### 缩放伪退出与事件坐标

- 对照独立实验的前后日志，确认修复前存在鼠标仍在窗口内却请求退出的输入，修复后该现象消失；这会使缩放目标在 1／0 之间来回切换。
- `inVisibleRect` 自动更新追踪区域；容器和按钮均保留同一 tracking area，不在每次缩放布局时重建。实时输入直接读取屏幕鼠标坐标，避免旧事件局部坐标加上新窗口原点。
- mouseExited／事件发给其他窗口只触发退出核验。矩形内还须用公开 `NSWindow.windowNumber(at:belowWindowWithWindowNumber:)` 核对最上层可点击窗口，避免遮挡后的悬停残留；窗口遮挡通知补充鼠标静止时的复核，无 AX 或持续轮询。
- 四向真实窗口检查覆盖伪退出、同屏幕点焦点稳定、追踪区域复用及高层非激活窗口遮挡；遮挡测试须等待 WindowServer 提交，并先恢复缩放目标以排除通知已提前收尾的假阳性。保留原有菜单、拖拽与隐藏状态保护及正弦／余弦算法。


### 高频输入与显示刷新

- 鼠标回调直接布局会把高频设备输入、重复追踪事件和动画计时器叠加为无效重绘；改用所在视图的公开 CADisplayLink，每帧合并最新屏幕坐标，与余弦过渡共用一次布局。静止 250ms 后暂停并复用链接，退出／拖拽／隐藏时清理；使用弱回调目标避免保留环。
- 相同焦点与进度跳过几何更新，窗口变形不强制同步显示。即时退出取消待提交输入，防止下一帧重新放大。
- 本机双图标独立窗口的 480 次突发输入，原逐事件布局约 177 ms；合并后含一次布局在本轮重复检查中约 1.8–6 ms。此项验证输入积压成本，不能当作真实帧率或端到端延迟；回归同时断言最后位置生效、空闲暂停、待提交输入可取消。


### 缩放期间脱离整条约束布局

- 突发输入耗时不能证明持续绘制流畅；`--check-dock-performance` 使用 12 个真实图标、混合标题和显示链接连续测量。当前 60 Hz 显示器上，原路径单帧同步工作 P95 约 12 ms，新路径约 7.5 ms；显示回调约 150 次／2.5 秒。该数据不代表 GPU 呈现帧率，也不能宣称已验证 120 Hz。
- 缩放时保留原视图和父子关系，只暂时移出 NSStackView 排列、暂停项目尺寸约束，直接应用正弦映射后的屏幕矩形；统一提交布局。项目访问统一读取快照，退出、拖拽、菜单收尾、隐藏和重建时恢复原排列约束。
- 悬停开始一次预留最大变形窗口，逐帧只改内部玻璃和图标，避免 WindowServer 重复调整窗口。透明预留区必须停止截获鼠标，跨应用监听负责重新进入；退出与自动隐藏按可见区域判断，不能按扩大的窗口矩形判断。
- 回归覆盖固定窗口、四向实际图标尺寸、公共圆点基线、徽章与分割线、透明区穿透及重新进入、遮挡、菜单、拖拽和隐藏恢复。模拟隐藏后须走真实 reveal 恢复窗口位置，仅恢复状态枚举会制造假失败。

### 静态悬停预览的会话与权限

- 预览归属复用 `DockModel.apps(onDisplay:)`，截图列表只用于 ID＋PID 关联，不能推断桌面归属。点击通过 Launcher 重新读取归属并走精确聚焦，避免复用 Dock 点击的“前台则最小化”切换语义。
- ScreenCaptureKit 异步取消不保证底层请求立即停止：全局保留一个工作任务，等待它返回后才启动最新待处理会话；令牌和取消检查共同阻止旧截图回写。每次展开一次捕获，不把截图或窗口枚举放进鼠标／动画回调。
- 关闭 NSPanel、清空视图引用不足以保证图片立即释放，AppKit 可能暂留旧视图；回归发现后改为先逐项清空 NSImageView.image，再移除内容。四向布局检查同时验证图片弱引用释放和会话令牌失效。
- 屏幕录制与辅助功能分开授权；预览默认关闭，关闭时不检查或申请捕获权限，只有设置按钮主动发起申请。诊断进程必须使用正式路径和稳定签名，独立原型的授权不代表正式 PS 已授权。
- 本机正式固定签名、通过 LaunchServices 正常启动的 PS 已确认两项权限均可用；从命令行直接执行同一二进制会将 AX 状态报告为 false，不能据此断言正式应用丢失授权。`--check-preview-capture` 仅针对 `local.ps.preview-validation` 独立测试应用：实测一张可见窗口缩略图、一项最小化占位，捕获不激活；最小化恢复、指定窗口置顶及隐藏恢复均通过。真实跨屏鼠标过渡仍需交互验证，不能由几何检查替代。
- 悬停的权限／错误提示不能使用固定文本框高度；先设置 NSPanel 内容尺寸，再安装内容视图；否则从零尺寸扩展时 autoresizing 会把文字挤出上边缘。按当前语言和可用宽度测量换行高度，再在屏幕边界内定位；回归同时检查子视图位于内容边界内。


### 显示刷新节拍不能逐帧暂停

- 侧对话真实鼠标 A/B 日志显示，每处理一帧就暂停 CADisplayLink 会不断重新唤醒；部分时段处理仅约 1–4ms，回调间隔 P95 仍达 180–330ms。持续回调的自动 CPU 基准绕过了此路径，不能据此宣称真实操作流畅。
- 正式输入只负责唤醒；移动期间每帧读取最新 `NSEvent.mouseLocation`，静止 250ms 且过渡结束后才暂停，离开后完成余弦退出并停止。保留遮挡命中和窗口预览跨间隙保护；未采样真实输入的固定几何预览独立处理。未引入实验中的基准位置缓存、图标预栅格化或标题改动。
- 最终实验真实输入记录的主要移动时段回调间隔 P95 约 17–20ms，用户已确认实验手感；这是该实验环境的记录，不能推算其他刷新率或正式版本端到端帧率。正式回归通过同一事件入口和实际 CADisplayLink，注入可控屏幕位置，在四向检查连续采样、最新坐标、静止停表、恢复和退出；此项是调度验证，不冒充真人鼠标体验。


### 全屏分区与运行／窗口状态分离

- 全屏属于 Space 元数据（现有系统类型 4），不能按窗口大小或仅按可见显示器 `isFullscreen` 推断。扫描全部显示器的 `Spaces`，窗口通过 `spaceIDs` 关联；未知类型不猜测。共享项用稳定窗口 ID 标记，先从本桌面窗口列表移除，再尾部追加，避免同一全屏窗口重复出现。
- `pid != nil` 只表示进程运行；本桌面的 `windowCount == 0` 不代表整个应用无窗口。独立保存全局窗口状态，按 bundle ID（缺省 PID）匹配，最小化和隐藏窗口保留；图标单独调暗，不能降低整个按钮透明度导致运行灯、徽章一起变暗。
- 共享全屏项允许用户明确点击后跨桌面聚焦，执行前重新核对窗口 ID、PID、应用标识和全屏 Space。复用精确置顶，不走普通 Dock 点击的最小化切换或无窗口新建路径；关闭窗口仍复用现有操作。元数据丢失或窗口已退出全屏时拒绝过时目标。

- macOS 27 实测：可见窗口的 ScreenCaptureKit 捕获正常，其他 Space 全屏窗口的新旧静态截图接口均返回 `-3811`；取消全局裁剪和偶数尺寸也未解决。旧公开 `CGWindowListCreateImage` 按窗口 ID 能取得离屏内容，因此只在已有屏幕录制权限、系统 27、符号存在且 ScreenCaptureKit 失败时回退；工作线程串行捕获，核对 PID、防窗口 ID 复用，输出缩略图后释放原图。受控测试确认 440×247 图像内容正确，桌面和前台应用保持不变，不以“返回非空图片”代替内容验证。

- 同一受控应用同时有普通窗口和其他 Space 的全屏窗口时，`AXWindows` 只返回普通窗口；仅按 PID 激活或原生 front-process 调用不能证明选中了指定窗口。明确点击时做限时 250ms 的 AX remote-token 补查，必须同时匹配窗口 ID 与 `AXWindow` 根节点，再复用现有精确置顶；当前私有布局仅启用 macOS 27.0 / 26A428，失败提示而不随意聚焦其他窗口。受控实测“离屏、正确内容、截图不改变桌面／前台、点击精确聚焦”四项均通过；未把此补查放进鼠标或常规刷新回调。

### 图标明暗不能直接使用原始窗口快照计数

- 企业微信关闭主窗口后仍保留无 Space 归属、不可见的大尺寸窗口对象；网易邮箱大师还会留下属于另一显示器当前 Space 的离屏对象。原始快照为窗口操作保留的候选对象不能直接表示“有打开窗口”。
- 明暗单独复用 phantom 判定，并纳入所有显示器的可见 Space；排除无归属或可见桌面上的离屏残留，保留最小化、隐藏、真正位于其他桌面的窗口，以及暂时缺失 Space 元数据的可见窗口。不改动进程运行灯、桌面归属、自动退出或启动策略。
- 正式签名进程的 `--check-window-presence <bundle ID>…` 可只读核对原始候选与有效窗口数；本次企业微信原始 3 项、邮箱大师 2 项均为残留，有效窗口均为 0，实际程序坞图标调暗且运行圆点保留。

### AX 提前接管窗口布局原型：接入前结论（尚未接入 PS）

- 复核 `/tmp/ps-ax-avoidance/Preview.swift`：Option＋绿色按钮、标题栏空白双击、Fn＋Control＋F/R/方向键及窗口菜单共用 `performLayout`，先拦截再直接向避让目标做 300ms 余弦过渡；这属于应用窗口布局接管，不会改变系统工作区。用户已确认多入口手感；安装的实验 App 与 `MultiEntryBuild` 的 `__text` 哈希一致。所给 `multi-entry.log` 仅有 `READY trusted=false` 和权限阻断，不能当作多入口成功日志。
- `button-fixed.log` 记录邮箱大师绿色按钮 `identity=false semantic=true` 后成功调整，证明不能单凭 `CFEqual` 判断按钮；应组合按钮 role/subrole、所属窗口 ID/PID 和命中范围。该日志 8 次结果中有一次还原失败；动画实际约 323–339ms、14–18 帧，单次 AX 写入 P95 约 15–23ms，不能以请求 120fps 宣称实际达到该帧率。
- 事件回调仍执行同步 AX 命中、父链遍历、尺寸及可写性查询；普通单击也会查菜单。命中查询的 50ms 限制不能等同整条回调预算，其他查询和遍历会累积。正式方案应由前台窗口／菜单通知准备有限缓存，回调只处理有明确证据的候选事件，设置整次截止时间；缓存失效或尚未就绪时放行，不为事后纠正补一次缩放。菜单取消成功前不得启动窗口修改，避免系统菜单与 PS 同时布局。
- `animationBusy` 为全局标志，忙时返回 true 会吞掉新命令；当前动画无取消令牌，用户拖动或关窗后仍可能写最终目标。需按窗口建立事务，以 PID＋进程启动身份＋窗口 ID 标识，新命令从当前实测位置接续最新目标；关闭、手动调整、切桌面、权限失效或停用时取消旧写入。动画写入串行、按时间跳过过时帧，减少动态效果仍直接应用目标。
- 原型还原失败也删除快照，显式 restore 未先核对是否已被用户手动修改，退出会排队等待当前动画后再恢复。正式实现仅在窗口身份和最后实际写入矩形仍匹配时恢复；成功确认后才清记录，受最小尺寸等约束失败时保留可解释状态，不反复强制重试。原型 `attach/enqueue/adjust` 未接入启动路径，是遗留的事后调整实验，不应随提前接管方案带入。
- 正式几何输入应来自对应显示器 PS Dock 的静止玻璃区域及距屏幕边缘距离，不能用含放大预留透明区的 NSPanel.frame。将此边界与系统 visibleFrame 求交，避免重复扣除系统 Dock／菜单栏；按显示器 UUID 管理，处理负坐标、上下排列及四向停靠。建议自动隐藏／不显示的 Dock 不预留常驻区域，悬停显隐不推动窗口；该行为需在接入阶段确认体验。
- 菜单标题的中英文对照不足以证明是系统平铺命令：需要核验完整菜单路径、可用状态及可取得的命令标识／快捷键信息，排除同名自定义项、多窗口布局和全屏命令；未知语言或无法确认的项放行。快捷键不能只靠固定键码假定仍是系统绑定；双击必须尊重实际系统设置。
- 接入前补齐各入口与还原的成功／失败记录，并逐项验证：连续不同指令及跨窗口输入、动画中拖动／关窗／退出、菜单取消失败与键鼠释放配对、事件 tap 超时恢复、权限缺失、应用尺寸限制、四向多屏和自动隐藏。绿色按钮悬停布局面板、边缘拖拽平铺、第三方改尺寸继续明确列为未覆盖；不引入全局事后追踪纠正。

### AX 原型复验：授权、识别覆盖与几何重复扣减（2026-09-19）

- 实验 App 反复丢失辅助功能授权，根因不是证书不受信任，而是 `codesign` 未带 `--keychain`：身份不在默认搜索列表时签名静默退化为 ad-hoc，cdhash 随每次重建变化，TCC 授权随之失效。`find-identity` 报 `CSSMERR_TP_NOT_TRUSTED` 属误报，带 `--keychain` 指向 `~/Library/Application Support/Powerspaces/Signing/local.keychain-db` 即可正常签名。指定要求稳定为 `identifier "local.ps.axavoidance.stable" and certificate leaf = H"ede24be8…"`，同一身份重签可跨重建保持授权。`sign-experiment.sh` 固化该流程并拒绝 adhoc 结果。
- 不能用整文件哈希或 CodeDirectory 页哈希判断「只换签名未换代码」：签名嵌入 Mach-O，`codesign` 会把签名槽清零后重算页哈希，ad-hoc 槽与真实身份槽大小不同（832B 对 18912B），填充字节落在首页内，页哈希必然变化。应改用行为判据（入口标识字符串在位、可正常启动并写出 `READY`）。
- 绿色按钮 AX 形态覆盖率（聚焦窗口、`depthLimit=7`）：Safari、Tabbit、腾讯会议均为 `AXButton/AXFullScreenButton`，上溯 1 级到 `AXWindow`，`identity=true semantic=true`，可识别；微信命中为 `AXWindow/AXStandardWindow`，既无 identity 也非 semantic，父链上不存在按钮节点，属明确不可识别。Finder、Obsidian、Ghostty 因被其它窗口遮挡未能判定。系统设置同属可识别形态，但实测命中查询返回了遮挡窗口的元素，说明授权判定必须校验命中元素的 PID 属于目标应用，遮挡即放行。
- 命中查询耗时（`CopyElementAtPosition`，各 100 次）：中位数 0.1–1.5ms，但 Safari p99 达 46.7ms、Code 达 48.9ms，更大样本曾出现 55.9ms 与 61.4ms。50ms 硬预算正好压在 p99 上，约 1% 的查询会被误拒而放行原操作。接入时应放宽到 80–100ms，或允许一次重试，并保持「无法确认即放行」的语义。
- 几何重复扣减已实测确认：主屏 `axVisible` 高 1331pt（系统已扣 109pt），原型 `allowed` 从 frame 底边扣 140pt 后再与 `axVisible` 求交，实际净预留 170pt，多扣 61pt；无 Dock 的副屏无此问题（净预留 140pt）。原型用 `visible.intersection(allowed)` 与既有结论「应与此边界求交，避免重复扣除」自相矛盾，正式实现必须按 Dock 静止玻璃区域相对屏幕物理边的距离计算预留，而不是叠加到 `visibleFrame` 之上。
- 自动化取证的测试框架教训：`NSRunningApplication.activate` 与随即投递的合成点击之间存在竞态，目标应用未真正前置时，点击会落到恰好覆盖该坐标的其它应用窗口上（本次曾命中正在使用的浏览器窗口）。用合成输入做验证时必须在前台状态确认后才投递，并先校验命中元素的 PID；该竞态是测试框架缺陷，不影响真实用户操作路径。

### AX 原型逐项验证结果（2026-09-19 第二轮，尚未接入 PS）

验证方式：以持久身份签名并授权实验 App，用事件 tap 的真实输入路径驱动，几何结果按原型 `performLayout` 规则独立复算后逐字节比对。

- **键盘入口（`Fn＋Control＋F/R/方向键`）通过**：`source=system-key` 成功接管，`fill` 与 `left/right/top` 实测几何与复算期望完全一致（`-0.0` 仅为浮点负零）。`bottom` 返回 `success=false`、`write size=-25204`，原因是目标高度 635pt 低于窗口可设最小值，属系统/应用约束而非缺陷；这类失败已正确记为失败且未强制回写。
- **还原语义通过**：正常 restore 精确回到初始矩形并 `recordRetained=false`；布局后手动改窗再按 restore，得到 `REJECT restore ownership`，窗口保持用户位置不动、记录保留，符合「身份与最后实际矩形匹配才恢复」的门槛要求。
- **连续输入接续通过**：150ms 内连发 `left` 后 `right`，`generation` 由 8 接续到 9，新命令从当前实测位置 `(208,148,1017,806)` 起算，最终几何等于 `right` 期望值——旧原型「忙时吞掉新命令」的问题已消失。
- **自动隐藏模拟通过**：以 AX 勾选「模拟自动隐藏」后，日志 `autoHide=true` 且目标高度为可用区域满高（副屏 1410pt），零常驻预留生效；系统最终给到 1409pt，差 1pt 为窗口尺寸取整。
- **多屏通过**：窗口移至副屏后，日志正确识别 `screen=(2560,0,2560,1440)`（未误用主屏状态），目标按副屏计算。
- **事件 tap 取消路径通过**：日志出现 `CANCEL reason=pointer-down`，指针按下时旧动画被取消，未继续写入。
- **命中查询存在两处真实限制**：① 遮挡时 `AXUIElementCopyElementAtPosition` 返回遮挡窗口的元素（实测为通知中心 `pid 657` 的浮动窗口，以及全屏的腾讯会议窗口），此时必须校验命中元素 PID 是否属于目标应用，不符即放行；② 即便命中正确按钮，仍可能返回 `hitError=-25204`（`kAXErrorCannotComplete`），日志显示 `CALLBACK elapsedMs=63.7`，即 50ms 预算被突破。综合前述 p99 数据，接入时应把预算放宽到 80–100ms 或允许一次重试。
- **窗口菜单入口可读但合成点击未能验证**：AX 可读取「窗口」菜单的完整项表、位置、可用状态与层级，`填充`／`缩放`／`居中`／`移动与调整大小`／`全屏幕平铺` 均在 `AXMenuBarItem(窗口) → AXMenu` 路径下，与原型排除「全屏幕平铺」的规则一致。但合成点击打开菜单后枚举下拉项不稳定，该入口与**标题栏双击**均留待真实手测。
- **`填充` 项可能被禁用**：Safari 窗口铺满屏幕时「填充」为 `禁用`，原型按 `enabled` 过滤后不接管并放行原操作，属正确行为；验证菜单入口需先选一个该项可用的窗口状态。
- 合成输入验证的固有风险：`NSRunningApplication.activate` 与随后投递的点击存在竞态，本环境有应用持续抢焦点。测试框架必须在前台状态确认后才投递并校验命中 PID，否则点击会落到恰好覆盖该坐标的其它窗口上。
- **标题栏双击入口未能取得可信证据**：Finder 上标题栏取点命中为 `AXButton`，被原型按角色规则拒绝（双击确认只接受 `AXWindow`／`AXTitleBar` 角色，且要求控制按钮位于该按钮中点 ±14pt 内）；Safari 上同一次测试命中为 `AXDockItem`，坐标明显有偏差。后者说明合成双击的取点或投递不可靠，因此这两个结果都不能用于判定该入口是否可用，必须真实手测。可复用的结论是：标题栏区域在部分应用报告为 `AXButton` 而非 `AXTitleBar`，双击识别不能只依赖 `AXTitleBar` 角色存在。
### AX 原型缺陷定位：身份标识与写入事务（2026-09-19 第三轮）

用事件 tap 真实输入路径采集，并在 `performLayout` 与菜单上溯路径加埋点后定位到两处实现缺陷，另有两条测试方法学结论。

- **`launchDate` 不能作为窗口身份标识的前置条件**：原型用 `"\(pid):\(launch.timeIntervalSince1970):\(id)"` 标识窗口，但 `NSRunningApplication.launchDate` 对部分进程恒为 `nil`——实测 Finder 全生命周期为 `nil`，Safari 有值。这使 Finder 上绿灯、标题栏双击、窗口菜单三条路全部被同一守卫挡死；即使在 Safari 上，11 次运行中也有 3 次取不到该字段（约 27% 失败率）。身份标识应只用 pid 与窗口 ID。修复后 Finder 上双击立即出现 `INTERCEPT source=title-double-click`。
- **位置与尺寸必须在分开的 AX 写入里分段提交**：日志反复出现 `write size=-25204 position=0`，即位置写入成功、尺寸写入被系统拒绝，动画随即以 `success=false` 中断并把窗口留在中间尺寸（用户观感是「动了但没到位」）。把两者放进同一次快速提交会被拒绝；改为「先位置、间隔约 100ms、再尺寸、再确认位置」并在动画结束时补写一次目标，而不是直接放弃。
- **合成输入与硬件输入的事件坐标在 tap 中不一致，不能互证**：真实鼠标点击送进事件 tap 时 `event.location` 为左上原点坐标（实测点击菜单项得到 `(74,202)`，与 AX 坐标一致，原型据此正确识别）；而 `CGEvent.post` 投递的同一屏幕点在 tap 中读回的是左下原点坐标（实测投递 `(1356,1294)` 对应真实位置 `(1356,146)`）。因此用合成事件复现硬件路径会得出错误结论——早期多次「命中 `AXDockItem`／`AXScrollArea`／`AXZoomButton`」的异常结果均源于此，不能据此判定原型缺陷。凡涉及事件坐标的验证必须以真实输入为准。
- **标题栏空白区在真实应用里极窄，双击识别不能只认 `AXWindow`／`AXTitleBar` 角色**：在 Finder（宽 900pt）与 Safari（宽 2545pt）上逐点扫描交通灯水平线，同一水平线上绝大多数命中为 `AXButton`、`AXStaticText`、`AXRadioButton/AXTabButton`；真正报告为 `AXWindow` 的只剩零散窄缝，最宽处约 12–16pt，Finder 上单段最长约 132pt。用户按直觉双击标题栏时通常命中标签页或工具栏按钮而被拒绝。正式实现应改为「先按几何判定该点位于标题栏带内（窗口顶边与最小化按钮底边之间，且不落在任一交通灯按钮及工具栏控件内）」，再结合命中元素的所属窗口校验，而不是要求命中元素本身是窗口或标题栏角色。
### AX 原型三入口全部跑通：菜单来源判据与两个稳定性缺陷（2026-09-19 第四轮）

本轮把入口③（窗口菜单）从「始终失败」调到「全部命令通过」，并修掉两个间歇性缺陷。最终统计：`fill` 8/8、`left`/`right`/`top`/`bottom`/`topLeft`/`topRight`/`bottomLeft`/`bottomRight` 各 1/1，拦截 16 次、成功 16 次、失败 0、无结果 0；几何逐字节等于按 `performLayout` 规则独立复算的期望值。

- **菜单来源判据：前两种写法均被实测证伪**。①「上溯父链遇到标题为『窗口』的 `AXMenuBarItem`」——Safari 的菜单父链从子菜单直接跳到工具栏分组（`AXGroup → AXButton → AXWindow → AXApplication`），链上根本没有菜单栏项；②「最外层菜单挂在 `AXMenuBar` 之下」——从最外层菜单上溯 24 层也未遇到 `AXMenuBar`。任何依赖菜单栏归属的校验在 Safari 上都不成立。
- **可用的来源判据是成员集合特征**：取最外层 `AXMenu`，其直属子项中必须命中至少两个系统布局命令成员（填充／居中／移动与调整大小／二等分／四等分／排列／全屏幕平铺／恢复上一个大小等），并配合「命中元素 PID 属于目标应用且非自身进程」。同时必须维护排除表：`全屏幕平铺`、`屏幕左侧/右侧`、以及 `左侧与右侧` 一类多窗口排列项一律放行，否则会接管系统原有行为。
- **歧义命令必须按「前一个分组标题」消歧**：Safari 在同一子菜单内用**禁用的**分组标题 `二等分`、`四等分`、`排列` 划分区段，`左侧/右侧/顶部/底部` 与 `左上/左下…` 分别落在前两个区段。不能看父菜单标题（父菜单无标题），要看该标题之前最近的分组标题，否则半屏与四分之一屏会互相混淆。
- **`AXCancel` 只对最内层菜单有效**：对最外层菜单调用返回 `errAECoercionFail(-25204)`，而原型把最外层菜单传给了取消逻辑，导致「菜单取消失败 → 不启动布局」。改为传入上溯遇到的第一个（最内层）菜单后取消成功。
- **菜单关闭会让应用重建窗口的 AX 元素**：实测 Safari 在 `INTERCEPT` 后约 38 ms 报 `window-unavailable`，持有的旧元素失效；即使重新按 pid 解析，仍会遇到「读取返回成功（`posErr=0`）且 subrole 正常、但取不到 position 值」的过渡态。修法是动画开始前重新解析聚焦窗口，并对几何读取做短暂重试。这两处修完后 `window-unavailable` 与读取失败均降为 0。
- **事件 tap 读到的鼠标坐标是左上原点，与 AX 坐标一致**：用 `CGEvent.post` 合成点击时必须按左上原点投递，不要按左下原点翻转。实测同一枚绿灯，翻转投递会偏移到另一个元素（命中 `AXGroup` 并被拒），不翻转则 `identity=true semantic=true` 通过。此前多轮「合成点击命中 `AXDockItem`／`AXScrollArea`／`AXZoomButton`」的异常结论均源于这一个错误，与原型无关。
- **子菜单项必须悬停展开后才出现在 AX 树中**：脚本化验证菜单命令要沿路径逐级 `mouseMoved` 悬停父项，再点击目标项；直接点击父项会关闭菜单而非展开。
### 窗口布局避让接入正式应用（2026-09-19）

原型验证通过后已按用户授权接入 PowerSpaces。实现分两层：`Sources/SpaceKit/WindowLayoutGeometry.swift`（纯几何与身份，可单测）与 `Sources/SpaceKit/WindowLayoutInterceptor.swift`（事件 tap、菜单识别、动画事务）；应用侧接线在 `AppDelegate.applyWindowLayoutInterception()`、`DockPanel.layoutReservation()` 与 `Sources/PowerspacesApp/DockReservationSource.swift`，开关为 `windowLayoutInterception`（**默认关闭**）。

- **预留量必须只扣系统尚未扣除的部分**。系统 `visibleFrame` 已为 macOS 自己的 Dock 与菜单栏留出空间，而 PowerSpaces 的程序坞常停在系统 Dock 所占的同一块区域。原型原本「从屏幕物理边扣 reserve，再与 visibleFrame 求交」，在主屏上因交集取严而使预留完全失效（实测可见区 y=30..1361、程序坞厚 78pt，交集后仍为 1331pt 高）。正确做法是逐边比较 `visibleFrame` 相对物理边已让出的量，只额外扣除 `reserve - 已让出`。主屏实测：程序坞 78pt 已在系统让出的 79pt 内 → 不额外预留；设成 140pt 时恰好多让 61pt，与原型阶段量到「多扣 61pt」的数值吻合。
- **预留在正式实现中改为量程序坞玻璃在屏幕上的实际内沿**。曾先后试过两种错误做法：只取 `barThickness()`（玻璃厚度，实测 78pt）会让窗口底边正好压在程序坞上；只用系统 `visibleFrame` 已让出的量推断，则完全测不出 PowerSpaces 程序坞（它停在系统让出区域之外）。正确做法是取玻璃视图 `effect` 在屏幕坐标下的实际 frame，换算成距对应屏幕边的距离（实测主屏 119pt：物理底边 1440 − 玻璃内沿 1321）。面板窗口比玻璃更高（含悬停放大余量），不能用面板 `frame`。
- **坐标约定的坑复现两次**：`CGEvent.post` 的合成事件在事件 tap 中按左上原点读取（与 AX 一致），不要做左下原点翻转；翻转后同一坐标会落到另一个元素上（绿灯翻转投递会命中 `AXGroup` 并被拒，双击翻转投递会让 `event.location.y` 偏出标题栏带）。真机硬件输入不受影响，只有自动化验证需要遵守。
- 三入口在正式应用内实测通过：Option＋绿灯、标题栏双击、窗口菜单「填充」均产生 `INTERCEPT → RESULT success=true` 并按几何带正确落位。`WindowLayoutDiagnostics`（`/tmp/ps-window-layout.log`）保留为落盘诊断，便于排查「识别到了但没有接管」。

正式接入后的实测值（主屏 2560x1440、程序坞停底部）：玻璃视图 `effect` 在屏幕坐标下为 `(701, 85, 1158, 78)`，即玻璃底边距屏幕底 85pt；系统 `visibleFrame` 底部已让出 35pt，故额外预留 50pt，可用区落到 `(0, 30, 2560, 1325)`，窗口底边距玻璃内沿仍有 16pt 间隙。副屏程序坞玻璃仅距屏幕底 6pt（几乎贴边）。三入口在该配置下实测一致落到同一目标矩形。

**预留量的正确来源（2026-09-19 修正）**：先后试过三种错误做法，最终定为**取程序坞面板自身的布局尺寸**（`DockPanel.panelSize()` 的横轴，实测底部停靠 129pt）：

| 做法 | 结果 |
| --- | --- |
| 只取 `barThickness()` 玻璃厚度（78pt） | 窗口压在程序坞上 |
| 取玻璃视图 `effect` 的屏幕内沿（6pt） | 更糟，等于没预留 |
| 用系统 `visibleFrame` 已让出的量推断 | 官方 Dock 隐藏后系统只留菜单栏，推断不出 PS 程序坞 |
| 读窗口 `frame` 反推 | 定位/动画过程中读回 y=0，与实际占位不符 |
| **取 `panelSize()` 横轴（129pt）** | 窗口底边 1311 = 程序坞顶边 1311，零重叠 |

官方 Dock 隐藏后主屏 `visibleFrame` 只剩菜单栏 30pt，程序坞完全落在系统让出区域之外，因此**必须按 PS 程序坞自己的尺寸预留**，不能再依赖系统几何。另确认：事件 tap 读到的鼠标坐标与投递坐标一致（左上原点），合成输入无需翻转。

### 程序坞预留的最终实现与外部改尺寸纠正（2026-09-20）

**预留量 = 屏幕物理边 → 可见玻璃外沿**，两个输入都必须取对：

- **位置取窗口服务器**：AppKit 的 window `frame` 在定位后与真实占位不符——实测面板读回 `(695, 0, 1170, 129)`（看似贴屏幕底），而窗口服务器 `CGWindowListCopyWindowInfo` 报 `(695, 1311, 1170, 129)`。必须按 `kCGWindowOwnerPID` + `kCGWindowNumber` 从窗口服务器取真实边界。
- **外沿取玻璃偏移，不取面板总高**：面板比玻璃高出一段看不到东西的透明带（实测 129pt 对 78pt），按面板总高预留会让窗口与可见程序坞之间空出约 50pt。玻璃在面板内的偏移由 `effect.convert(effect.bounds, to: nil)` 得到，各边外沿 = 面板对应边 + 玻璃在该侧的偏移。
- 实测结果：玻璃顶边 = 1311 + 6 = 1317，从屏幕底边 1440 量起预留 **123pt**；窗口底边落在 1317，像素级测量与玻璃顶边差 **0.0pt**。

**外部改尺寸的纠正**（第三方最大化、Option＋拖动）：这两类操作不经过事件 tap，只在接管成功后才装 `AXObserver` 观测该窗口的 `kAXWindowMovedNotification` / `kAXWindowResizedNotification`，几何稳定后若有窗口压住预留带就把它收进可用区：

- 只纠正**已被接管过**的窗口，不触碰用户自由摆放的其它窗口。
- 纠正时保持左右位置与顶边，必要时同时收高度——第三方最大化会把窗口设成整屏高，只上移会把它顶出屏幕（实测 `target=(-0.0, -93.0, 2560.0, 1410)`，`fits=false`）。
- 观测回调有 0.25s 合并窗口，动画进行中（`activeTargets` 非空）不打断。

实测：整屏高的窗口被子最大化后自动收回 `(0, 30, 2560, 1287)`；窗口落在程序坞上（底边 1300 > 可用区底边）不再压住。

### 程序坞预留的最终结论：量到「可见玻璃内沿」（2026-09-20）

预留 = **屏幕物理边 → 可见玻璃内沿**。位置与尺寸两个输入都必须取对，最终实现为：

- **位置**：窗口服务器 `CGWindowListCopyWindowInfo`（按 `kCGWindowOwnerPID` + `kCGWindowNumber` 匹配）。AppKit 的 window `frame` 在定位后不可靠——实测读回 `(695, 0, …)`（看似贴屏幕底），实际是 `(695, 1311, …)`。
- **内沿**：玻璃视图 `effect` 在面板内**贴外侧**，多出的留白在内侧。因此可见玻璃内沿 = 面板外沿 + `(面板长度 − 玻璃长度 − 玻璃外侧偏移)`，**不是**面板外沿本身。

实测（内建屏 1470×956、底部停靠）：`panel=(6,827,1457,129)`、`effectFrame=(6,6,1445,78)`、`barThickness=78`，玻璃内沿 = 827 + (129−6−78) = **872**，预留 = 956−872 = **84pt**。

弯路记录（都对不上，勿重走）：

| 取法 | 结果 | 现象 |
| --- | --- | --- |
| 面板外沿（`panel.minY`） | 827 → 预留 129pt | 窗口与程序坞之间留约 45pt 空隙 |
| 面板尺寸（`panelSize()` 横轴） | 同上 | 同上 |
| 玻璃偏移当作从顶边算 | 833 → 预留 123pt | 仍留约 39pt 空隙 |
| **面板外沿 + 内侧留白** | **872 → 预留 84pt** | 窗口底边正好压在程序坞玻璃上沿 |

验证方式：截图像素逐行扫描，窗口白色内容止于 870、程序坞玻璃自 872 起；三入口窗口底边均为 872。

### 缩短外部改尺寸后的可见闪烁（2026-09-20）

**原理限制**：事件 tap 只能在**动作发生前**接管（点击/按键被拦下后由我们直接动画到目标，系统的最大化不执行），因此三种入口没有"弹一下"。而第三方最大化与 Option＋拖动是**外部进程直接改窗口几何**，只能事后纠正，无法完全消除闪烁——目标是把它压到人眼几乎察觉不到。

实测与优化：

| 项 | 优化前 | 优化后 |
| --- | --- | --- |
| 观测去抖 | 250ms | **40ms** |
| 端到端纠正延迟 | 约 296ms | **约 40–60ms（2–4 帧）** |

去抖只用于合并同一次操作产生的多条 `kAXWindowMoved/Resized` 通知，不再等待几何"稳定"。另外新增**指针按下状态跟踪**（`pointerIsDown`，由事件 tap 的左键按下/抬起维护）：

- 指针按下期间（拖动/缩放中）**不纠正**，避免与用户抢几何、造成抖动；观测通知在按下期间直接忽略。
- 左键抬起时立即纠正一次（延迟 0），所以拖动全程不受干扰、松手瞬间贴合。

实测：Option＋拖动全过程只产生 1 次纠正且发生在松手后；第三方最大化 3 次测量的端到端延迟 95–122ms（其中 `./lat` 进程启动占约 50–80ms，应用侧实际约 40–60ms）。两条路径最终窗口底边均为 872，与三入口一致。

### 外部改尺寸的纠正不再依赖先触发入口（2026-09-20）

**问题**：入口 4、5（第三方最大化、Option＋拖动）必须先触发一次入口 1～3 才生效。原因是 `AXObserver` 只在**接管成功时**才安装（`beginLayout` 内），而这两条路径不经过事件 tap——没有被接管过就没有观测，纠正逻辑拿不到窗口。

**修法**：观测改为**主动覆盖前台应用的全部标准窗口**，不等接管。`observeFrontmostApplication()` 在三处调用：`start()` 之后、应用激活/启动/退出通知（`refreshAction`）、屏幕参数变化（`screensChanged`）。每个 pid 一个 `AXObserver`，逐窗口注册 `kAXWindowMoved/Resized`。

**纠正规则收紧为「只有变大导致的越界」**，避免变成"怎么摆都管"：

- 以 `observedFrames[identity]` 记录上次几何；仅当**高度增加**且越界超过 4pt 才纠正。
- 只处理当前**确实压住程序坞预留带**的窗口。
- 实测：手动缩小到程序坞上（`[300,800,900,220]`）与移动到程序坞上（`[300,900,900,220]`）**都不被移动**；变大越界则纠正到底边 872。
- 之所以敢放宽到这个程度：macOS 隐藏自带 Dock 后，系统 `visibleFrame` 已让出菜单栏以下的整块区域（实测底边 923），PS 程序坞玻璃内沿（872）在其**内部**，所以纠正只会把窗口推到「系统本来就认为可用」的范围里，不会侵占用户可自由使用的空间。

配合前述 40ms 去抖与指针按下状态抑制，五条路径最终都落到同一底边 872，且入口 4、5 无需任何前置操作。

### 四方向停靠的窗口避让测试（2026-09-20）

在底部、左、右、顶部四种停靠下逐一实测「三入口 + 外部最大化」，四个方向的窗口最终几何都收敛到同一预期值。过程中修掉三个只在非底部方向暴露的缺陷——**这些都逃过了只测底部的验证**：

- **左右两侧的预留公式写反**：原实现用「到对侧屏幕边的距离」，左停靠算出 1333pt（半个屏宽），窗口直接压在程序坞上。正确做法是从**本侧**屏幕边量到玻璃内沿：左侧量到玻璃右边界，右侧量到玻璃左边界。
- **外部改尺寸的越界判据只看了竖向**：`snapOffDock` 原本只比较 `maxY`，左右停靠时窗口是横向越界，纠正完全失效。改为按停靠方向分支：底部看 `maxY`、顶部看 `minY`、左侧看 `minX`、右侧看 `maxX`。
- **左/右的越界方向不能用同一条边界**：左侧坞的预留带在屏幕左边，右边界本来就等于可用区右边界，因此要看 `minX` 是否压进预留带；右侧坞相反，要看 `maxX`。顶部坞还必须在**下移的同时收高度**，否则底边会超出屏幕（实测 156+923=1079 > 956）。

四个方向的实测结果（内建屏 1470×956）：

| 停靠 | 程序坞面板 | 预留 | 窗口最终几何 |
| --- | --- | --- | --- |
| 底部 | `(6, 827, 1458, 129)` | 84 | `(0, 33, 1470, 839)` |
| 左侧 | `(0, 38, 313, 914)` | 176 | `(176, 33, 1294, 923)` |
| 右侧 | `(1157, 38, 313, 914)` | 307 | `(0, 33, 1163, 923)` |
| 顶部 | `(6, 33, 1457, 129)` | 156 | `(0, 156, 1470, 800)` |

几何自洽性用像素复核：左侧窗口左边界 176 = 玻璃内沿 176；右侧窗口右边界 1162.5 ≈ 玻璃内沿 1163。注意左右停靠时玻璃只占面板的一小部分（170pt 对 313pt），余下是悬停放大朝屏幕内侧的余量，因此那一段不可见留白不计入预留。

### 可重复验证入口与它抓出的 bug（2026-09-20）

新增两个命令行入口，A`AGENTS.md` 的构建与验证一节点名它们：

- `--check-window-layout`：窗口避让的确定性自检，退出码 0/1。覆盖四个停靠方向的
  预留换算（用内建屏实机测得的四组面板/玻璃几何作回归基准）、可用区包含关系、
  外部最大化后的纠正落点，以及「变小与移动不纠正」。断言调用的是生产路径上的同一份
  纯函数（`DockGeometry.reserveThickness` / `WindowLayoutCorrection.correction`），
  不是另写一份复算。
- `--dump-window-layout`：打印当前程序坞的面板（窗口服务器）、玻璃、屏幕与推导出的预留，
  便于在任意停靠方向下人工核对。

**这次自检在写好之后立刻抓出一个真 bug**：左右停靠时，外部最大化后的整屏窗口**宽度**
超出可用区（1470 > 1294），而纠正只做平移不缩尺寸 → 直接放弃纠正。修法是四个方向都
把尺寸收进可用区；左右分支还漏了纵向收敛（只设 x 不设 y，导致目标 y=0 小于可用区
顶边 33 而被守卫拒绝）。这条缺陷与先前那三个一样，只有非底部方向才会触发。

把公式抽成纯函数的过程也暴露了一件事：`glassInPanel`（玻璃视图在面板内的 frame）
在四个方向之间**参考边并不一致**（底部报 6 而实际内沿在 45、右侧报 137 而实际在 6），
因此任何「按统一规则推广」的写法都会算错——试过「偏移取两侧较小值」后底部算成 911、
右侧算成 137、顶部算成 39。最终保留按方向各自取面板对应边的形式，并用实机像素测量
反推出的四个期望值锁定，不再做代数推广。

### 多窗口应用：拆分 / 合并两种呈现（2026-09-20）

新增偏好 `windowDisplayMode`（`split` 默认 / `merged`），设置页位于 Windows 标签的「多窗口应用」。

- **拆分**（默认，保持既有行为）：每个窗口一个图标，开启窗口标题时以标题命名。
- **合并**：每个应用一个图标，圆点数量**严格等于当前桌面的窗口数**；
  当前桌面无窗口但仍在运行时显示**一个空心圆**（表示没有完全退出）；
  已退出、仅因固定而保留的条目**不显示圆点**，沿用现有灰显。

实现要点：

- **拆分门控必须同时覆盖两条路径**。窗口标题模式本身也会拆分图标，因此合并模式
  不能只关「每窗口一图标」。判断抽成 `DockRefresher.expandsPerWindow(mode:iconPerWindow:windowLabels:)`
  纯函数并单测，避免以后又只改一半。
- **合并模式关闭窗口数量角标**（圆点已经表达同一信息），保留通知徽章不受影响。
- **圆点规则抽成 `DockButton.indicator(mode:isLauncher:isRunning:windowCount:)` 纯函数**，
  由 `--check-merged-indicators` 覆盖 0–4 个窗口、运行但无窗口、已退出与启动器五种情形。
- 多圆点复用原有的单点视图：数量为 1 时几何与拆分模式完全一致，只在 n>1 时沿停靠轴
  按一个圆点直径的间距排开并整体居中，因此四向停靠与缩放下的位置沿用既有逻辑。
- 空心圆在 `AdaptiveDockMark` 上以描边实现（线宽按尺寸收敛，避免小圆点被糊成实心）。
- 「当前桌面窗口数」沿用既有 `DockApp.windowCount`（已在既有测试中锁定跨桌面隔离），
  合并模式不新增任何窗口归属判断。

实机对照（同一桌面，Safari 3 窗口 / Obsidian 2 窗口）：合并模式下 Safari 为 1 个图标 +
3 个圆点、Obsidian 为 1 个图标；拆分模式下 Safari 3 个图标、Obsidian 2 个图标，各带 1 个圆点。

**补充（同日）**：拆分模式的窗口标题标签需要按显示模式门控。首版只在标签应用处判断，
遗漏了条目宽度计算那处，导致合并模式下仍会残留 `~/projects/` 这类宽条目。
现统一走 `DockRefresher.labelsWindows(mode:isLauncher:windowCount:)` 纯函数并纳入单测。
另外记录一条环境注意：`showWindowLabels` 的**默认值是 `true`**（拆分模式即「每窗口一个图标 +
以窗口标题命名」）；用偏好文件做验证后要恢复默认，否则会误以为拆分模式丢了窗口名。

### 空心圆改为空心胶囊（2026-09-20）

「应用在运行、但**当前桌面没有窗口**」的标记由空心圆改为**空心胶囊**（14×4pt 描边、圆角取短边一半），方向跟随停靠方向：顶/底停靠为横向、左/右停靠为纵向。空心圆与实心圆点在 4pt 尺寸下区分度不足，胶囊明显更容易辨认。

同时把这条规则的适用范围从合并模式**扩展到两种模式**：拆分模式下同样用它表示「没退出但本桌面没有窗口」。规则集中在一处：

```swift
DockButton.indicator(mode:isLauncher:isRunning:windowCount:vertical:)
// 非启动器且在运行 → windowCount == 0 时空心胶囊；否则合并模式给 N 个实心点，拆分模式给一个实心点
```

实现上给 `AdaptiveDockMark` 增加了 `.capsule` 形状（`init(capsule:)`），切换形状时更换标记视图而不是改属性；标记尺寸按形状取值（胶囊沿停靠轴 14pt、横轴 4pt），布局循环改用它，因此横向与纵向的定位沿用原有分支。

自检 `--check-merged-indicators` 已覆盖：0–4 个窗口、空心胶囊的横向与纵向、两种模式下的「运行但无窗口」、已退出无标记、启动器无标记。

### 跨 Space 移窗在本机不可用（2026-09-20 实测）

为「单实例应用在其他桌面」增加「搬移至当前桌面」策略时，先验证底层能力，结论是**本机 macOS 27 上做不到**：

| 路径 | 结果 |
| --- | --- |
| `CGSAddWindowsToSpaces` / `CGSRemoveWindowsFromSpaces` | 符号存在（SkyLight 与 CoreGraphics 均可 `dlsym`），但调用**无任何效果** |
| `CGSSetWindowListWorkspace` | 返回 `kCGErrorIllegalArgument (1006)` |
| 窗口菜单 | 无「移到桌面」类菜单项（Safari / Finder 均只有「将标签页移到新窗口」） |
| 系统 Dock 项的 AX 属性 | 只有 `AXPress` / `AXShowMenu` / `AXShowExpose`，没有桌面分配属性 |

判别实验最能说明问题：**把窗口从它当前所在的 Space 移除**都毫无变化（`spaces(of:)` 仍返回原 Space），而同一套读接口（`CGSCopySpacesForWindows`，mask `0x7`）工作正常。也就是说这不是参数或顺序问题，而是**写接口在本机被禁用或未实现**——写入不做任何事，也不报错，因此无法靠返回值判断失败。

验证环境：内置屏单显示器、两个普通桌面（ManagedSpaceID 3 与 7）+ 一个全屏空间（489）；测试工具已确认 `AXIsProcessTrusted=true`，排除权限因素。

外部资料印证了这一点（同一结论已被主流窗口管理器确认）：

- [yabai #2500 "Moving windows without SIP disabled stopped working since Sequoia"](https://github.com/koekeishiya/yabai/issues/2500) —— 自 macOS 15 起，**不关闭 SIP 就无法用这套私有接口移窗**。
- yabai 的 [Disabling System Integrity Protection](https://github.com/asmvik/yabai/wiki/Disabling-System-Integrity-Protection) 说明：它的 `window --space` 等空间操作依赖关闭 SIP 的脚本附加件。
- [DockLift 的 SpaceMover](file:///Users/ethanlee/Documents/Codex/2026-09-18/referenced-chatgpt-conversation-this-is-an/work/DockLift-SpaceMover.swift) 也自述这是 best-effort：用 `CGSMoveWindowsToManagedSpace` / `CGSAddWindowsToSpaces` 之后**必须用公开的 `CGWindowList` 复核**，因为「on recent systems the CGS path may silently no-op for windows the process does not own」。我按它的写法（`CGSMoveWindowsToManagedSpace(cid, windows, spaceID: UInt64)`）复测，仍为 no-op——与 SIP 未关闭一致。

**更正（同日）**：以上只说明「**窗口级**」那套接口不可用。真正可用的是**进程级**接口——
见下一节，`CGSProcessAssignToSpace` 在 SIP 开启下工作正常。探针代码已全部撤销；`AGENTS.md` 中「跨 Space 移窗尚未实现」的判断在本机仍然成立。若将来在关闭 SIP 的机器上重试，应先写一个只做「从当前 Space 移除」的判别实验——它比端到端搬移更快证伪，且不会留下半成品。

### 跨 Space 移窗的正解：进程级 `CGSProcessAssignToSpace`（2026-09-20）

前一轮否掉了「窗口级」接口（`CGSAddWindowsToSpaces` / `CGSMoveWindowsToManagedSpace` 在 SIP 开启时静默 no-op），但**系统 Dock 的「选项 → 分配给 → 这个桌面」是能做到的**。顺着这条线在 Dock 二进制里找到了它真正调用的接口：

```bash
nm -u /System/Library/CoreServices/Dock.app/Contents/MacOS/Dock | grep -i space
#   _CGSProcessAssignToSpace
#   _CGSProcessAssignToAllSpaces
```

`CGSProcessAssignToSpace(cid, pid, spaceID)` 是**进程级**语义：改变「该进程的窗口属于哪个桌面」，因此已有窗口立即跟过去，后续新窗口也落在那里。与窗口级那套完全不同，**在 SIP 开启的系统上可用**。

实测（内建屏，Desktop 1 = space 3、Desktop 2 = space 7）：

| 对象 | 结果 |
| --- | --- |
| ChatGPT 窗口（单实例） | `spaces=[3]` → `[7]`，来回 3 次稳定 |
| Tabbit（多窗口） | 两个窗口**同时**跟着改（进程级语义的直接体现） |
| PS 坞点击端到端 | 在 Desktop 2 点 PS 坞的 ChatGPT 图标 → 窗口从 space 3 过来并聚焦（`onscreen=true`） |

实现要点：

- `WindowSpaceMover`（`Sources/SpaceKit/WindowSpaceMover.swift`）用 `dlsym` 动态解析该符号，系统移除时照常启动、策略降级为警告；`isAvailable` 供 UI 判断。
- **接口不返回错误**，因此必须按结果确认：搬移后重新读取该 pid 全部窗口的归属，必须**恰好**等于目标 Space，否则抛错。`CGSSpaceProvider.spaces(forPID:)` 提供这个复核。
- 新策略 `.moveHere`「搬到本桌面」：单实例应用在其他桌面时，点击即把它搬过来再聚焦——不新建实例、不关闭应用（区别于 `quitReopen`）、也不把用户带走。失败降级为警告。
- 与「Control+点击跳转」互补：前者搬窗口过来，后者跳过去；都由用户选择。

**仍属私有接口**：本机 macOS 27.0 (26A428) 验证通过，不保证未来版本；所有调用点都带降级路径，不宣称长期兼容。

界面上该策略标注为「搬到本桌面（实验性）」，页脚说明它依赖未公开的系统接口、可能在未来的 macOS 版本失效；中英两份资源同步维护（`--check-localization` 会核对键的一致性——本轮就抓到过一次中英页脚 key 不一致，靠它才发现）。

「搬到本桌面」在三个入口都可选，均带「（实验性）」标记与同一句页脚说明：

| 入口 | 位置 |
| --- | --- |
| 设置 · 通用 | 「未知应用的默认策略」下拉 |
| 设置 · 单实例应用 | 单个应用的下拉 |
| 程序坞右键 | 「已在其他桌面打开时」子菜单 |

实测（`AXPress` 驱动 PS 坞图标的右键菜单）：点击「已在其他桌面打开时 → 搬到本桌面（实验性）」后 `config.json` 从 `"strategy": "warn"` 变为 `"moveHere"`，说明菜单项与设置项走同一条写入路径。

### 激活应用时把它的窗口搬到当前桌面（2026-09-20）

系统设置关闭「切换到应用程序时，切换到有打开窗口的空间」（`AppleSpacesSwitchOnActivate = 0`）后，激活已在他桌面的应用不再跳转；本功能补齐最后一环——把那个应用的窗口搬到当前桌面。

**检测方式（关键教训）**：本机 macOS 27 上 `NSWorkspace.didActivateApplicationNotification` 与 `activeSpaceDidChangeNotification` **都不送达**——不只是应用收不到，独立进程监听 `NSWorkspace.notificationCenter` 的全部通知也是零回调（前台确实变了）。因此检测落在**已有的有限轮询**里：只比较相邻两次前台 pid（`ActivatedAppMover.ForegroundChange`），不新增计时器。

**「切桌面」的排除**：不能用时间窗。实测 `open -a` 触发的激活可能发生在启动后 53ms，此时"最近一次 Space 变化"就是启动时那次 nil→初始值的记录，时间窗会把正常激活误判成切桌面。改为**直接比较 Space ID**：与上次判定时的 Space 不同就不搬（`lastCheckedSpaceID`）。

**实机验证**（内建屏，Desktop 1 = space 3、Desktop 2 = space 7；用「系统设置」作对象，`open -a` 触发激活与 Spotlight 等价）：

| 场景 | 结果 |
| --- | --- |
| 窗口在桌面 2、当前在桌面 1，激活 | `spaces=[7] → [3]`、`onscreen=true`，桌面未变 |
| 已在当前桌面，再次激活 | `[7] → [7]`，不重复搬（幂等） |
| 窗口在桌面 2、切到桌面 1 后激活 | 留在 `[7]`，未搬（Space 变化守卫生效） |
| 关闭开关后同样操作 | 留在 `[7]`，不动 |
| 系统开关设为 1 时 | 系统未按预期跳转（本机该设置似乎不生效），但功能仍会搬；界面据 `AppleSpacesSwitchOnActivate` 给出提示 |

**只读系统设置**：`SpacesSwitchOnActivate` 只读不写。判定抽成纯函数 `decide(stored:)`（缺失按开启），因为 `UserDefaults(suiteName:)` 仍会回退全局域，真实系统上无法构造"缺失"这一输入——这一点是被 `--check-activated-app-move` 抓出来的。

新增检查：`swift run --build-system native PowerspacesApp --check-activated-app-move`（偏好默认关闭、可持久化、系统开关三种状态）。

### 停止接管 Ctrl＋方向键（2026-09-20）

窗口避让原本接管 `Fn＋Control＋F/R/方向键`（对应系统单窗口平铺命令），但用户反馈"Ctrl＋方向键变成了调整窗口，原来是切桌面"。排查结论与处置：

- **不是系统设置被改**。系统 Mission Control 的空间切换快捷键（`com.apple.symbolichotkeys` id 79–82）仍为 `enabled = 1` 且无自定义参数；PS 从不写该 plist，只用 `CGSSetSymbolicHotKeyEnabled` 在内存里临时禁用，而那条路径（`fasterKeyboardSwitch`）默认关闭、当时也未开启，`spaceHotkeysDisabledByUs` 为 `false`。纯 `Ctrl＋方向键` 在任何系统绑定里都不存在。
- **根因是修饰键判断在键盘之间不可靠**。实测用户在**未按 Fn** 的情况下，`Ctrl＋↑/→` 的事件里 `maskSecondaryFn` 为真（`flags=10748161`，`ctrl=true fn=true`）——部分外接键盘的方向键自带该标志。事件层无法把它与"用户真的按了 Fn"区分，于是普通 Ctrl＋方向键被当成平铺命令吞掉。
- **处置：把方向键从接管列表移除**，只保留 `Fn＋Control＋F`（填充）与 `R`（还原）。方向键交回系统；`windowLayoutInterception` 开关保留在设置页，只调整了说明文案。
- 实测确认：`Ctrl＋←` 不再被拦截（无 `INTERCEPT`、窗口不动）；`Fn＋Control＋F` 仍正常产生 `INTERCEPT source=system-key command=fill`。
- 教训：**凡是靠修饰键区分意图的接管，都要考虑键盘差异**。方向键、以及某些键盘上的功能键，可能在不按键的情况下携带修饰标志；这类接管必须实测多种键盘，或干脆不接管。
- 附带修正自检的环境依赖：`--check-window-layout` 里原本硬编码了按 1470×956 记录的面板坐标，屏幕换成 2560×1440 后误报。现改为按当前屏幕尺寸构造物理自洽的合成几何（面板贴屏幕边、玻璃贴面板外侧、外侧偏移 6pt），期望值统一为 `外侧偏移 + 玻璃厚度`，与停靠方向和屏幕尺寸都无关。

#### 修正：通知是送达的，之前"通知不可用"的结论是错的（同日）

我先前测出 `NSWorkspace.didActivateApplicationNotification` 不送达，**那是测量错误**：当时被激活的应用已经是前台，没有产生"前台换人"事件。独立进程复核后确认通知正常送达——**同一应用重复激活不送通知**，这是它唯一的坑。

据此把检测改回**通知优先**（并在通知里同步搬移，抢在系统切桌面之前）：

```
NOTIFY app=com.openai.codex  space=7  windows=["11792:[3]"]   ← 通知到达时窗口仍在桌面1
outcome=moved                                                  ← 同步搬移，桌面不跳
```

**纯轮询版本为什么在 Spotlight 场景失败**：轮询太慢。用户日志里 `outcome=spaceChanged` 的那次，系统已经先把桌面切到了窗口所在的 space，轮询醒过来时判定"切桌面了→不搬"——表现就是"跳转而不搬移"。轮询仍保留为兜底（通知不送时不至于完全不动作）。

教训：**"通知不可用"这类结论必须用独立进程、在确实发生状态变化时复核**，否则会把测量错误当成平台限制写进文档。

### 程序坞前景色改为「整块一次判定」（2026-09-20）

**问题**：圆点/胶囊与分割线颜色不统一，实测同一块玻璃上出现「白胶囊 + 白圆点 + 黑圆点 + 黑分割线」。

**根因**：`AdaptiveDockLabel` 让**每个元素各自**用 `CABackdropLayer` 采样自己那一小块背景来决定纯黑/纯白。圆点背后是图标映上来的亮玻璃、分割线是 1pt 细线采到暗色，于是同一块玻璃上判定结果不同。
注意：**取色规则本身是一致的**，不一致的是每块采样的**输入**。

**修法**：判定上移到 `GlassSurfaceView`（整块玻璃一次采样），由面板 `DockPanel` 统一下发给所有自适应元素：

- `AdaptiveDockLabel.unifiedTextColor(luminance:previous:)` 是纯函数，带迟滞（0.57 / 0.43）。
- `applyUnifiedTextColor(_:)` 下发后**关闭该元素的逐元素采样**（移除 backdrop 层），避免又退回旧行为。
- 每次内容重建后补发一次，覆盖新建的圆点/胶囊/标题。
- 采样区指向**指示灯条带**（`DockButton.indicatorBand` → `GlassSurfaceView.luminanceRegion`），
  而不是整块玻璃：整块含图标区，图标映上来的亮色会把判定带偏。
- 手动指定标题颜色时（`automaticTitleColor == false`）不改动任何元素。

实测（同一应用状态，逐元素测量指示灯行的像素）：

| 元素 | 修改前 | 修改后 |
| --- | --- | --- |
| 空心胶囊 | 白 | 黑 |
| 圆点 | 白（其中 1 个黑） | 黑 |
| 分割线 | 黑 | 黑 |

**后续更正（同日，最终方案）**：先试过「固定纯白」，但对照原版程序坞后发现原版本来就是**墨色跟随背景**（不是玻璃材质——实测原版指示灯是实心纯色小胶囊，约 4×8pt，无高光无描边；`NSGlassEffectView` 默认圆角 8.0，在此尺寸下根本无法成形）。

最终实现**两档墨色 + 迟滞**，判定仍由面板按**整块**玻璃做一次（逐元素采样会导致白圆点配黑分割线）：

| 背景亮度 | 墨色 | 分离度 |
| --- | --- | --- |
| ≥0.55（浅色玻璃） | 深墨 0.10 | 0.45–0.90 |
| ≤0.45（深色背景） | 白 | 0.45–1.00 |

**为什么不用连续灰阶**：中亮度背景下任何灰都与背景分离不足（数学上无解），指标会「消失」。切换点因此放在分离度最差处（中灰 ≈0.5），并加 0.45/0.55 迟滞避免临界闪烁；实测最差分离度仍有 0.40。

顺带修掉一个检查自身的缺陷：`NSColor.white` 属灰阶色彩空间，直接读 `brightnessComponent` 会抛 `NSInvalidArgumentException`，必须先用 `usingColorSpace(.sRGB)` 转换。

### 右键菜单显示不全（2026-09-20）

**现象**：应用图标的右键菜单只显示约 3 项，底部出现滚动箭头。

**根因**：菜单原先用 `menu.popUp(positioning:at:in: button)` 弹出，**锚定在程序坞面板里的按钮视图上**，AppKit 因此把菜单压在面板窗口范围内。

量化（实测，非推测）：

| 数据 | 值 |
| --- | --- |
| 应用菜单项数（源码统计） | 25 项 |
| 25 项菜单实测高度 | ≈545pt |
| 程序坞面板高度 | 129pt |

545pt 的菜单被压进 129pt 的视图 = 约 3 项 + 滚动箭头。

**修法**：改为 `in: nil`（屏幕坐标）弹出，并用 `button.window?.convertToScreen(...)` 把按钮矩形换算到屏幕坐标；水平停靠时按图标在屏幕上下半区决定往哪边展开，并夹在 `visibleFrame` 内避免贴边。菜单不再属于面板窗口，因此可超出面板高度、完整显示。

`--check-dock-ink` 增加几何断言：完整菜单高度必须大于面板高度，且弹出方式必须是屏幕坐标（`contextMenuIsPresentedOffView`）。

### 对照原版程序坞的观察（2026-09-20）

用系统 Dock（`tilesize=72`）实拍对照，得到两条可用于本项目的结论：

1. **原版指示灯不是玻璃材质**：是**实心纯色小胶囊**（约 4pt 宽 × 8pt 高、两端圆角），无高光、无描边、无透光。判据之一是 `NSGlassEffectView` 的默认 `cornerRadius` 为 8.0，在 4pt 宽的尺寸下无法成形。
2. **原版墨色跟随背景**：浅色玻璃上近纯黑；窗口最大化导致玻璃变纯白时转为**灰**——这是「墨色随背景」而不是「玻璃」，与标题文字的取色思路同类。

据此把本项目的指示灯从「固定白色」改为「两档墨色跟随背景」。

**未验证项（如实记录）**：原版指示灯**精确到小数点的尺寸与圆角**没能测出——自动检测反复把图标内容/壁纸纹理误判成指示灯（本机上窗口内容与程序坞区域重叠）。上文的「约 4×8pt」来自放大截图的目视判读，不是像素级精确值。若要精确测量，需要先铺一张纯色壁纸以拉开分离度。

### 右键菜单：三次修正的完整教训（2026-09-20）

同一个问题改了三轮，每轮都以为找到了原因，值得完整记录下来。

**症状**：应用图标的右键菜单只显示 3 项 + 底部滚动箭头。

| 轮次 | 做法 | 结果 | 为什么错 |
| --- | --- | --- | --- |
| 1 | `popUp(in: button)` → `in: nil` + 屏幕坐标 | 仍 3 项 | 以为"锚定视图"是约束来源，其实**容器参数才是** |
| 2 | 改 `popUpContextMenu`（附带合成事件） | **12 项全显示**，但位置跑到屏幕左下 | 该入口位置由鼠标位置决定，不可控；且我误判"修好了" |
| 3 | 回到 `popUp`，容器传**按钮**，锚点做屏幕→窗口换算 | **12 项 + 位置正确** | 正解 |

**最终结论**：`NSMenu.popUp(positioning:at:in:)` 的 `in:` 为 **nil 时菜单会被压到约 3 项**（AppKit 找不到宿主窗口，退化成受限弹出）。必须传入宿主视图。

**两个把我带偏的测量错误**（都已写进诊断习惯）：

1. **合成右键不移动真实鼠标**：`NSEvent.mouseLocation` 返回的是工具进程的位置（实测值 `y=45`，而图标在屏幕底部）。凡是读"当前鼠标位置"的代码在自动化验证下都会拿到错误值。改用**按钮自身的屏幕矩形**作为锚点，确定性且不依赖鼠标。
2. **`point` 的语义**：`popUp(positioning:at:in:)` 把该点当作**菜单左上角**向下展开。我原先按"图标附近的一个点"理解，于是 246pt 的菜单从 y=125 画到 y=-121，只有上面约 3 项在屏内——看起来就像"只显示 3 项"。现在按"菜单底边贴住图标上沿"计算。

**方法论**：定位 UI 弹出问题时，**先截图确认看到的到底是菜单的哪一部分**（顶部还是底部、是否真的是滚动），再动代码。我第二轮就是把"菜单底部露出了 3 项"当成了"菜单只有 3 项"。


### 全屏窗口纳入悬停预览（2026-09-20）

**现象**：pin 住的应用（`pins.json` 的 `[all desktops]`）若有全屏窗口，悬停图标看不到预览，显示"不在此桌面"。

**根因（两处叠加，缺一不可）**：

1. `DockModel.fullscreenItems` 生成全屏分区时**排除已 pin 的应用**（`!(pinnedBundleIDs.contains)`）——所以 pin 的应用没有独立的全屏条目。
2. 普通条目的预览走 `WindowPreview.windows`，**只按当前桌面的 Space 筛选**。全屏在 macOS 里独占一个 Space，于是被排除，预览列表为空 → 显示"不在此桌面"。

**修法**：

- `WindowPreview.windows` 增加 `fullscreenSpaceIDs` 参数（调用方注入 `provider.fullscreenSpaceIDs()`）：除当前桌面窗口外，**全屏 Space 的窗口也纳入**。参数带默认值，旧调用方行为不变（有单测锁定）。
- 新增 `WindowPreview.isFullscreen(_:fullscreenSpaceIDs:)` 作为唯一判定，卡片标记与点击分支共用。
- 截图改为 `allowOffscreen: true`：全屏 Space 不可见，不允许离屏就只会在卡片上写 "Window is on another desktop"，正是本功能要消除的情况。该开关同时启用 `ignoreGlobalClipSingleWindow`，离屏也能拿到完整内容。
- 预览卡片右上角新增 **Liquid Glass 胶囊包裹应用图标**（macOS 26+ 用 `NSGlassEffectView`，旧系统回退 `NSVisualEffectView` + 圆角）标识全屏窗口；点击走 `focusFullscreenWindow`（允许切到全屏所在 Space，与共享全屏分区同一条路径），而不是普通预览的"只聚焦、不切桌面"。

**新增诊断命令**：`powerspaces preview <bundleID>` 打印某应用会进入预览的窗口（含全屏标记），避免靠反复悬停验证。实测：

```
$ powerspaces preview com.microsoft.VSCode
active space 3  fullscreen spaces [489]  display D2D54920
预览窗口 2 个:
  win 19424  spaces=[489]  onscreen=false  [全屏]
  win 27461  spaces=[489]  onscreen=false  [全屏]
```

修复前这两个窗口都被排除（列表为空 → "不在此桌面"）。

**未完成验证**：预览浮层的**实机渲染**（玻璃徽章外观）我没能确认——悬停需要真实鼠标移动，合成 `mouseMoved` 不足以触发；computer-use 也列不到 PS（accessory 应用、无标准窗口，`COMPUTER_APP_NOT_FOUND`）。数据源、判定逻辑与编译路径已验证，**渲染外观需人工悬停确认**。

### 60fps 优化先区分真实输入与独立基准（2026-09-22）

- 本轮基于用户回退后的 `6960224`，未恢复远端三次缩放试错。原独立基准已得到 150 次 / 2.5 秒回调、间隔 P95 16.79ms；这条合成焦点路径不能证明真实操作无卡顿。
- 真实缩放显示链接以最高 60Hz 为预算（屏幕不足 60Hz 时从屏幕上限），保持移动期间采样、静止 250ms 停表。暂停排列时不再更新失效的按钮宽高约束，直接设帧并显式刷新子视图布局；回归应检查实际 bounds，不能用失效约束的常量判断放大。四方向布局／进出／拖拽／菜单／遮挡及空闲恢复检查通过，SpaceKit 394 项断言通过。
- `--trace-dock-frames` 启动参数仅在真实鼠标采样会话中记录帧间隔、回调耗时、超过 25ms 的间隔数；每段最多 600 帧，暂停／退出后后台汇总到 `/tmp/ps-window-layout.log`（`dock-frames` 行），不记录鼠标坐标。正常启动不采集。它测量主线程回调节拍，不能等同屏幕实际呈现帧率。
- 优化后独立基准间隔 P95 16.69–16.78ms、处理 P95 6.06–7.17ms，有测量波动，尚无稳定提速结论；正式真实鼠标数据和用户手测仍待补齐，不以单次较快结果宣称达成稳定 60fps。

- 第一轮用户真实滑动记录为 473 帧、间隔 P95 35.39ms、88 次超过 25ms，回调处理 P95 仅 4.77ms；用户同时确认进出更卡。五秒调用栈采样显示主线程定时器执行 `refresh → snapshot → CGS/AX` 同步查询，独立绘制基准未覆盖它。
- 周期轮询改为单在途后台串行快照，主线程仍负责模型归属和 UI／副作用；显式刷新递增 generation、休眠使结果失效，超过一秒或活动 Space 不符的结果丢弃。未延迟用户显式刷新、未恢复远端“放大期间跳过重建”的试错。后续只读采样确认快照处于 `powerspaces.poll-snapshot` 队列；四向回归与 394 项断言通过，真实滑动复测待用户操作。
- `dock-transition` 汇总单独记录进入准备和退出恢复耗时，避免把一次性的长帧藏在稳态 P95 内；当前仍未证明进出掉帧已解决。

- 第二轮用户真实滑动：两段较长会话分别 377／368 帧，间隔 P95 19.36／19.70ms，超过 25ms 为 8／10 次（约 2–3%），比第一轮改善但仍有长间隔；快速进出会话仍出现 29–38ms 的 P95。进入准备实测约 0.25–0.95ms、退出恢复约 0.5–1.64ms，仅统计同步工作，不能据此排除后续 AppKit 绘制／提交成本。
- 后台快照之后，主线程采样仍出现 `DockModel.arrange → appName → FileManager.displayName → LaunchServices`。周期刷新把应用名称也在后台按本次快照解析（包含固定项），主线程只查结果字典；不引入永久名称缓存，显式事件刷新保持原行为。此后版本需继续实测，不宣称进出长帧已消除。

- 第三版日常使用日志可保留进出同步耗时，但缺少至少 180 帧的长滑动段；发现重建／隐藏从 `tick` 外调用 reset 时未封口统计，下一会话把空闲混成最大帧间隔（出现 176 秒）。新链接清时间戳、reset 显式封口后再做对比，旧跨会话最大间隔不可作为真实卡顿证据。

- **更正固定 60Hz 上限（实测）**：用户本轮显示器为 100Hz，修正会话统计后 439 帧／约 8.8 秒稳定落在 50fps。独立非激活窗口对比：`minimum=maximum=preferred=60` 实际 49.999Hz；三者 100 实际 99.997Hz。先前以 60Hz 屏的基准给高刷屏硬加 60 上限引入了节拍下降，现恢复跟随所在屏幕的原生刷新率，保留后台读取与布局优化。此实验验证显示链接调度，不代表完整 Dock 已达到 100fps。

### 缩放命中必须排除透明预留（2026-09-23）

- `NSPanel.frame` 和 `magnificationVisibleFrame` 仍含朝屏幕内侧的图标放大余量；直接用于进入判断，会让鼠标在上方应用窗口内也唤醒缩放。命中改为可见玻璃或已放大按钮的实际矩形，透明空隙不算命中，窗口遮挡判断继续保留。
- 鼠标穿透同时覆盖静止与放大状态；沿用现有全局／本地鼠标监听恢复命中，不增加轮询。自动隐藏使用可见内容包围范围；四方向回归新增透明余量不唤醒、可穿透检查，连同进入退出、菜单拖拽、覆盖、隐藏、指示灯和徽章检查全部通过。

### PS 覆盖原生 Dock 的桌面分配（2026-09-23）

- 搬移窗口只改变 Space 归属，不保证它自动升到当前窗口之上。确认搬移后应复用精确窗口的 AX 置顶：PS 程序坞点击先激活应用再置顶；前台激活自动搬移直接置顶，避免二次激活再次触发切桌面。记忆保存失败但窗口已搬成功时仍置顶；搬移失败则不抢焦点。
- 双屏回归：程序坞点击时虽然用点击屏幕的 Space 分类，`.moveHere` 执行层曾重新采用快照的 `activeSpaceID`。Music、ChatGPT 恰好各自在该 Space，便直接激活而未搬移；日志均为 `alreadyHere`。目标 Space 必须传到搬移层；进程分配成功后还须按点击屏幕移动窗口的物理坐标，否则跨屏窗口可能留在屏幕外。PS 程序坞已发起的搬移不应被随后到达的全局激活通知重复搬回主屏。
- 用户在更新后按原路径复测：左屏 Music、右屏 ChatGPT 均能搬移并自动置顶。

- 进程级 `CGSProcessAssignToSpace` 只影响当前进程，不能跨退出保存。用户要求 PS 的搬移覆盖原生分配，不能用“原生分配优先”的跳过条件。
- `com.apple.spaces/app-bindings` 保存稳定 Space UUID；Music 实际 bundle ID 为 `com.apple.Music`，Dock 键为 `com.apple.music`。修改按 bundle ID 忽略大小写，保留其他应用的记录。
- **只修改偏好文件不足以立即生效**：音乐的记录从桌面 180 改为 5 后，正常退出重启仍落在 180。Dock 反汇编确认还调用 `CGSSessionSetCurrentSessionWorkspaceApplicationBindings(connection, dictionary)`；会话字典值为数字 Space ID，`AllSpaces` 转为 `Sticky`，不可解析 UUID 不加入会话。
- 同步会话返回 0 后再次退出重启音乐，当前桌面仍是 180，新音乐窗口 5372 实际位于桌面 5；无需退出或注入系统 Dock。正式实现仅在已验证的 macOS 27 启用，动态探测接口；窗口搬移成功后再保存，失败单独报告，不把“窗口已移动、记忆未保存”当成未移动。
- 本轮验证为普通桌面和音乐应用；全屏 Space 不作为持久目标。系统 Dock 自身后续修改分配仍可能更新系统会话，下次 PS 搬移再以 PS 目标覆盖。
- 正式 `powerspaces open com.apple.Music` 复测：窗口 5372 从桌面 5 移到 180，偏好 UUID 同步改为 180；音乐正常退出后新窗口 5410 仍在 180。398 条断言与本地化检查通过；未把未经中间读数确认的切桌面操作当作跨桌面冷启动证据。

- **冷启动必须单独覆盖**：`notRunning → launchApp → coldLaunch` 绕过移窗分支。上轮“已运行后移窗，再退出重启”没有验证“用户换桌面后从 PS 冷启动”，不能替代用户路径。`moveHere` 冷启动先保存点击桌面的绑定，后台启动，再确认进程归属后激活；点击桌面的 Space ID 必须传递到底层，不能重新套用主屏当前 Space。
- 复测音乐：先退出并将旧绑定设置为桌面二（180），通过系统快捷键切桌面一并独立确认 ID=5，然后调用正式 `Launcher.dockClick`。记录为 `before 5 running false → launched → after 5 windows [(5583, [5])]`。此前自动化 AX 图标点击未触发启动，因此该证据明确属于正式点击入口的实机调用，未冒充鼠标点击端到端验证。

### 原生 Dock 自动尺寸调研（2026-09-23）

- Apple 的 `device-management/mdm/profiles/com.apple.dock.yaml` 分别声明 `tilesize` 与 `largesize`（设置范围 16–128）；这是配置范围，不能据此断言拥挤布局的实际最小图标尺寸。来源：https://github.com/apple/device-management/blob/release/mdm/profiles/com.apple.dock.yaml 。
- 本机 macOS 27 Dock 的 Objective-C 元数据存在独立的 `desiredTileSize`、`tileSize`、`largeSize`、`actualLargeSize`，以及 `tileInset`、`distanceBottom/Top/Sides`、`_setMaterialLayerFrame:tileSize:`。可确认期望尺寸、实际尺寸、悬停尺寸与材质几何分开管理；仅凭这些符号不能声称已还原完整空间约束公式或边距常数。
- PS 当前 `naturalStackSize()` 累加按钮、分割线、标题和间距，`barThickness()` 又取 `dockHeight` 与内容厚度的最大值；缺少按所属屏幕可用长轴空间求实际静止尺寸的步骤。只限制窗口宽度会裁切，只缩图标则会留下过厚玻璃。
- 同款行为的实现方向：保留用户设定值，按静止内容求不溢出的有效尺寸，并据此联动玻璃、间距、指示灯与交互几何；悬停动画不能反过来触发静止尺寸求解，以免放大/缩小反馈抖动。准确屏幕边距、极端拥挤下限及厚度曲线尚待原生 Dock 对照测量，不能把近似公式标成苹果原算法。

- 正式自适应布局使用静止内容总长（含标题、两处分区分割线、间距和端部留白）计算每屏有效比例；用户设定尺寸保持不变。玻璃厚度、标题内边距和指示灯间距随同比例变化，指示灯本身保留可见尺寸。屏幕端部保留 12 点为 PS 的实现取值，未宣称是苹果私有常数。
- 悬停最大扩张按现有正弦算法的两端位移预留；禁止从放大中的窗口宽度反算比例。跨适配阈值的项目增减直接采用最终布局，避免原有加入/移除宽度动画与整体缩放竞争；真实布局自检新增四向、标题模式、80 项拥挤与恢复检查。
- 验证：403 条 SpaceKit 断言通过；`--check-dock-layout` 四向 × 标题开关拥挤/恢复均通过（80 项侧边布局长轴 869 点，屏幕预算 923 点，图标 9→39 点），完整交互检查零失败；`--check-window-layout` 四向避让检查通过。极端拥挤允许继续缩小以维持全部项目可见，未把公开配置的 16 点误当作强制布局下限。

- 整体大小滑块独立持久化为 `dockScale`（0.5–2，默认 1）；实际比例为用户整体倍率乘以屏幕适配系数。布局预算必须先乘用户倍率，再求适配，避免拖大后溢出；不连写图标、间距、厚度细项，防止多次拖动累积误差。放大倍率与整体大小并排显示于普通设置，动画速度保留在高级选项。

- 七个已知单窗口应用的设置由逐应用 Picker 改为一个统一 Picker。旧 `config.json` 若存在不同的个人规则，界面显示“已有单独设置”，不启动时迁移或静默覆盖；用户选择统一行为后一次保存七项覆盖，保留其他应用和各项附加参数。批量选择 `quitReopen` 只询问一次且不即时退出任何应用。

### 程序坞状态过渡（2026-09-23）

- 同一组图标的进程、窗口状态变化原先触发整排按钮重建，运行指示、无窗口调暗和悬停缩放因而一起跳变。布局与槽位不变时应原位更新按钮；仅项目增减才走已有插入／移除动画。数字通知徽章只对纯数字在短暂显示链接内插值，`99+` 与文字保持原文；标记、徽章显隐和面板显隐用 AppKit / Core Animation 过渡，尊重系统“减少动态效果”。
- 本机 SDK 中 `CADisplayLink(target:selector:)` 不可用于 macOS；应通过 `NSView.displayLink(target:selector:)` 创建并在过渡结束或视图移除时停止。隐藏系统 Dock 会使 `visibleFrame` 短时变化；PS 隐藏原生 Dock 时按物理屏幕边定位，切换期间先淡出、重排后淡入，避免 PS 面板跟着临时工作区跳到底边。

### 单实例应用点击后没有界面（2026-09-23）

- `NSRunningApplication.activate()` 只激活进程，不能保证已关闭／最小化的窗口重新出现；通用点击也不能只凭前台 PID 判定“再次点击应最小化”，必须确认所选窗口确为 AX 主窗口。`.moveHere` 没有真实窗口时应先同步桌面分配，再请求应用重新开窗。
- 进程级 Space 分配确认早于窗口可由 AX 操作。音乐实机复测中，原窗口 ID 7668 从桌面 180 搬到 311 后短暂从快照消失，随后以同 ID 出现在 311；搬移后应有限等待窗口可访问，再调整跨屏坐标并精确置顶，失败明确提示。仅凭分配接口成功不能宣称界面已被唤出。
- 微信 `com.tencent.xinWeChat` 不支持以新实例可靠唤窗，纳入统一单实例设置。已有七项若全是同一个明确选择，运行时让微信继承且不改写旧配置；混合选择或微信单独配置则保持原意。
- 自绘图标把动作放在 `mouseDown` 时，`NSButton` 默认的辅助功能“按下”只触发未接线的 `action`，无法代替真实点击。应把 AXPress 也接到相同的应用路由，便于辅助功能操作与端到端复测。
- 微信实机出现同一进程的窗口分属桌面 180 与 311；搬移前须优先查找目标桌面已有窗口，不能因其他桌面仍有窗口就搬移整个进程。`CGSProcessAssignToSpace` 没有同步完成保证，确认归属需有限重试；曾在点击后立即确认时误报“无法搬移”。
- ChatGPT 的单个真实窗口位于右屏桌面 311 时，直接把进程分配到左屏桌面 180，等待 0.5 秒后归属仍为 311；先通过 AX 把窗口移入左屏再重试，同一接口确认归属变为 180，随后可精确置顶。跨屏分配失败时仅对单真实窗口做这项有限重试，并检查位置写入；失败恢复原位置，避免扰动多窗口应用。
- 多屏程序坞的新建窗口路径也必须传递点击屏幕的 Space ID。只用主屏 `activeSpaceID` 筛选新窗口时，应用在错误屏幕生成的真实窗口会被误当成完成；应按新窗口 ID 排除旧窗和无 Space 占位窗，移动物理位置后按目标 Space 确认，且仅在新进程只有一个真实窗口时尝试进程级分配。QSpace 右屏点击实测：新窗口 1094 属于桌面 6，窗口左边界 x=2560（右显示器起点），左屏原窗口仍在桌面 5。
